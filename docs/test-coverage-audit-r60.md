# Test Coverage Audit (r60)

_Generated by `cmd/audit-test-coverage` from a `go test -coverprofile=...` run._

## Overall

- **Total packages analyzed:** 70
- **Total files analyzed:** 1520
- **Total statements:** 137132
- **Total covered:** 62139 (45.3%)
- **Undertested packages (<50%):** 34
- **Never-tested files (0%):** 84

## Undertested Packages (<50% statement coverage)

Sorted ascending by coverage; lowest-coverage packages first.

| Package | Coverage | Covered / Total | Files |
|---|---:|---:|---:|
| `internal/ai` | 0.0% | 0 / 109 | 1 |
| `cmd/hexdek-odin` | 0.0% | 0 / 213 | 1 |
| `cmd/hexdek-artfetch` | 0.0% | 0 / 182 | 1 |
| `cmd/hexdek-ceiling` | 0.0% | 0 / 370 | 1 |
| `cmd/hexdek-composition-replay` | 0.0% | 0 / 69 | 1 |
| `cmd/hexdek-contrib` | 0.0% | 0 / 154 | 1 |
| `cmd/hexdek-heimdall` | 0.0% | 0 / 426 | 1 |
| `cmd/hexdek-heimdall-scanner` | 0.0% | 0 / 74 | 1 |
| `cmd/hexdek-huginn` | 0.0% | 0 / 156 | 1 |
| `internal/telemetry` | 0.0% | 0 / 16 | 1 |
| `cmd/hexdek-thor-crossref` | 0.0% | 0 / 198 | 1 |
| `internal/ws` | 0.0% | 0 / 275 | 1 |
| `cmd/hexdek-muninn` | 0.0% | 0 / 172 | 1 |
| `internal/party` | 0.0% | 0 / 350 | 1 |
| `scripts/find_high_freq_uncovered` | 0.0% | 0 / 79 | 1 |
| `cmd/hexdek-parity` | 0.0% | 0 / 33 | 1 |
| `cmd/gen-handlers` | 0.0% | 0 / 792 | 1 |
| `cmd/ceiling-check` | 0.0% | 0 / 64 | 1 |
| `cmd/hexdek-judge` | 0.0% | 0 / 438 | 1 |
| `cmd/hexdek-tournament` | 0.0% | 0 / 211 | 1 |
| `cmd/hexdek-valkyrie` | 0.0% | 0 / 274 | 1 |
| `cmd/reset-elo` | 0.0% | 0 / 38 | 1 |
| `cmd/tournament-audit` | 0.0% | 0 / 249 | 1 |
| `cmd/validate-composition-prior` | 0.0% | 0 / 118 | 1 |
| `cmd/hexdek-loki` | 3.4% | 24 / 703 | 1 |
| `cmd/hexdek-thor` | 13.6% | 2010 / 14784 | 40 |
| `cmd/hexdek-server` | 14.1% | 29 / 206 | 1 |
| `cmd/hexdek-import` | 21.2% | 63 / 297 | 1 |
| `internal/analytics` | 27.1% | 417 / 1538 | 10 |
| `internal/paritycheck` | 35.7% | 104 / 291 | 1 |
| `cmd/hexdek-oracle-sync` | 36.0% | 155 / 430 | 1 |
| `internal/game` | 36.2% | 226 / 624 | 5 |
| `internal/gameengine/per_card` | 37.6% | 17869 / 47571 | 1023 |
| `internal/gameast` | 43.9% | 68 / 155 | 5 |

## Never-Tested Files (0% statement coverage)

Production source files where every block missed in this run. Sorted alphabetically.

| File | Statements |
|---|---:|
| `cmd/ceiling-check/main.go` | 64 |
| `cmd/gen-handlers/main.go` | 792 |
| `cmd/hexdek-artfetch/main.go` | 182 |
| `cmd/hexdek-ceiling/main.go` | 370 |
| `cmd/hexdek-composition-replay/main.go` | 69 |
| `cmd/hexdek-contrib/main.go` | 154 |
| `cmd/hexdek-freya/legality.go` | 146 |
| `cmd/hexdek-freya/mechdb.go` | 181 |
| `cmd/hexdek-freya/statistics.go` | 121 |
| `cmd/hexdek-heimdall-scanner/main.go` | 74 |
| `cmd/hexdek-heimdall/main.go` | 426 |
| `cmd/hexdek-huginn/main.go` | 156 |
| `cmd/hexdek-judge/main.go` | 438 |
| `cmd/hexdek-muninn/main.go` | 172 |
| `cmd/hexdek-odin/main.go` | 213 |
| `cmd/hexdek-parity/main.go` | 33 |
| `cmd/hexdek-thor-crossref/main.go` | 198 |
| `cmd/hexdek-thor/advanced_mechanics.go` | 1499 |
| `cmd/hexdek-thor/adversarial.go` | 171 |
| `cmd/hexdek-thor/apnap.go` | 65 |
| `cmd/hexdek-thor/ast_fidelity.go` | 329 |
| `cmd/hexdek-thor/cascade_torture.go` | 170 |
| `cmd/hexdek-thor/chaos_games.go` | 281 |
| `cmd/hexdek-thor/claim_verifier.go` | 612 |
| `cmd/hexdek-thor/clock_pressure.go` | 150 |
| `cmd/hexdek-thor/combo_demo.go` | 128 |
| `cmd/hexdek-thor/combo_pairs.go` | 92 |
| `cmd/hexdek-thor/commander.go` | 98 |
| `cmd/hexdek-thor/corpus_audit.go` | 593 |
| `cmd/hexdek-thor/coverage_depth.go` | 171 |
| `cmd/hexdek-thor/deep_rules.go` | 1668 |
| `cmd/hexdek-thor/density_stress.go` | 170 |
| `cmd/hexdek-thor/graveyard_storm.go` | 185 |
| `cmd/hexdek-thor/infinite_loop.go` | 157 |
| `cmd/hexdek-thor/keyword_matrix.go` | 32 |
| `cmd/hexdek-thor/layer_stress.go` | 120 |
| `cmd/hexdek-thor/mana_verify.go` | 125 |
| `cmd/hexdek-thor/multiplayer_chaos.go` | 181 |
| `cmd/hexdek-thor/negative_legality.go` | 424 |
| `cmd/hexdek-thor/opponent_autodetect.go` | 148 |
| `cmd/hexdek-thor/oracle_compliance.go` | 185 |
| `cmd/hexdek-thor/oracle_diff.go` | 96 |
| `cmd/hexdek-thor/replacement.go` | 133 |
| `cmd/hexdek-thor/rollback_torture.go` | 167 |
| `cmd/hexdek-thor/spell_resolve.go` | 43 |
| `cmd/hexdek-thor/stack_torture.go` | 77 |
| `cmd/hexdek-thor/symmetry.go` | 180 |
| `cmd/hexdek-thor/turn_structure.go` | 146 |
| `cmd/hexdek-thor/zone_chains.go` | 104 |
| `cmd/hexdek-tournament/main.go` | 211 |
| `cmd/hexdek-valkyrie/main.go` | 274 |
| `cmd/reset-elo/main.go` | 38 |
| `cmd/tournament-audit/main.go` | 249 |
| `cmd/validate-composition-prior/main.go` | 118 |
| `internal/ai/autopilot.go` | 109 |
| `internal/analytics/cooccurrence.go` | 223 |
| `internal/analytics/matchup_detail.go` | 68 |
| `internal/analytics/resource.go` | 18 |
| `internal/db/deck.go` | 24 |
| `internal/db/device.go` | 14 |
| `internal/gameast/ability.go` | 4 |
| `internal/gameengine/chaos.go` | 142 |
| `internal/gameengine/counter_threshold.go` | 33 |
| `internal/gameengine/damage_replacement.go` | 25 |
| `internal/gameengine/keywords_batch2.go` | 107 |
| `internal/gameengine/keywords_batch5.go` | 381 |
| `internal/gameengine/reflexive_triggers.go` | 13 |
| `internal/hat/octo.go` | 153 |
| `internal/hat/selfplay.go` | 69 |
| `internal/heimdall/seed_binary.go` | 118 |
| `internal/hexapi/contrib_spotcheck.go` | 35 |
| `internal/hexapi/huginn_adapter.go` | 11 |
| `internal/hexapi/muninn_adapter.go` | 21 |
| `internal/hexapi/telemetry_adapter.go` | 3 |
| `internal/hexapi/versioning.go` | 28 |
| `internal/moxfield/parser.go` | 35 |
| `internal/moxfield/resolver.go` | 59 |
| `internal/party/handler.go` | 350 |
| `internal/telemetry/ga4.go` | 16 |
| `internal/tournament/audit.go` | 6 |
| `internal/tournament/chunk.go` | 42 |
| `internal/tournament/roundrobin.go` | 186 |
| `internal/ws/handler.go` | 275 |
| `scripts/find_high_freq_uncovered/main.go` | 79 |

## Per-File Recommendations

731 file(s) below the 50% threshold. Recommendation is architecture-aware (engine paths get higher priority than utility code).

| File | Coverage | Recommendation |
|---|---:|---|
| `cmd/ceiling-check/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/gen-handlers/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-artfetch/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-ceiling/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-composition-replay/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-contrib/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-freya/legality.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-freya/main.go` | 8.1% | minimal coverage — backfill primary path; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-freya/mechdb.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-freya/report.go` | 31.1% | below threshold — backfill edge cases; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-freya/statistics.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-heimdall-scanner/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-heimdall/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-huginn/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-import/main.go` | 21.2% | minimal coverage — backfill primary path; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-judge/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-loki/main.go` | 3.4% | minimal coverage — backfill primary path; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-muninn/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-odin/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-oracle-sync/main.go` | 36.0% | below threshold — backfill edge cases; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-parity/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-server/main.go` | 14.1% | minimal coverage — backfill primary path; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor-crossref/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/advanced_mechanics.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/adversarial.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/apnap.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/ast_fidelity.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/cascade_torture.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/cast_path.go` | 35.3% | below threshold — backfill edge cases; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/chaos_games.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/claim_verifier.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/clock_pressure.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/combo_demo.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/combo_pairs.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/commander.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/conditional_setup.go` | 48.9% | below threshold — backfill edge cases; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/corpus_audit.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/coverage_depth.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/deep_rules.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/density_stress.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/goldilocks.go` | 0.9% | minimal coverage — backfill primary path; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/graveyard_storm.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/infinite_loop.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/keyword_matrix.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/layer_stress.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/main.go` | 10.2% | minimal coverage — backfill primary path; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/mana_verify.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/multiplayer_chaos.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/negative_legality.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/opponent_autodetect.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/oracle_compliance.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/oracle_diff.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/replacement.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/rollback_torture.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/spell_resolve.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/stack_torture.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/symmetry.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/turn_structure.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-thor/zone_chains.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-tournament/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/hexdek-valkyrie/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/reset-elo/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/tournament-audit/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `cmd/validate-composition-prior/main.go` | 0.0% | never tested — add at minimum a happy-path test; CLI entry point — consider integration smoke test or extract logic to a tested helper |
| `internal/ai/autopilot.go` | 0.0% | never tested — add at minimum a happy-path test; internal utility — add unit tests for the public surface |
| `internal/analytics/analyzer.go` | 1.3% | minimal coverage — backfill primary path; Heimdall analytics — add a replay-fixture test |
| `internal/analytics/cooccurrence.go` | 0.0% | never tested — add at minimum a happy-path test; Heimdall analytics — add a replay-fixture test |
| `internal/analytics/matchup_detail.go` | 0.0% | never tested — add at minimum a happy-path test; Heimdall analytics — add a replay-fixture test |
| `internal/analytics/report.go` | 10.1% | minimal coverage — backfill primary path; Heimdall analytics — add a replay-fixture test |
| `internal/analytics/resource.go` | 0.0% | never tested — add at minimum a happy-path test; Heimdall analytics — add a replay-fixture test |
| `internal/db/deck.go` | 0.0% | never tested — add at minimum a happy-path test; internal utility — add unit tests for the public surface |
| `internal/db/device.go` | 0.0% | never tested — add at minimum a happy-path test; internal utility — add unit tests for the public surface |
| `internal/db/gauntlet_runs.go` | 32.3% | below threshold — backfill edge cases; internal utility — add unit tests for the public surface |
| `internal/db/party.go` | 38.4% | below threshold — backfill edge cases; internal utility — add unit tests for the public surface |
| `internal/db/showmatch.go` | 34.1% | below threshold — backfill edge cases; internal utility — add unit tests for the public surface |
| `internal/game/engine.go` | 7.4% | minimal coverage — backfill primary path; internal utility — add unit tests for the public surface |
| `internal/gameast/ability.go` | 0.0% | never tested — add at minimum a happy-path test; AST parser — add a representative card-text round-trip test |
| `internal/gameast/effects.go` | 26.8% | below threshold — backfill edge cases; AST parser — add a representative card-text round-trip test |
| `internal/gameast/filter.go` | 42.9% | below threshold — backfill edge cases; AST parser — add a representative card-text round-trip test |
| `internal/gameengine/chaos.go` | 0.0% | never tested — add at minimum a happy-path test; core engine path — high-priority, exercises rules code |
| `internal/gameengine/companion.go` | 37.5% | below threshold — backfill edge cases; core engine path — high-priority, exercises rules code |
| `internal/gameengine/conditional_parser.go` | 21.9% | minimal coverage — backfill primary path; core engine path — high-priority, exercises rules code |
| `internal/gameengine/cost_modifiers.go` | 26.8% | below threshold — backfill edge cases; core engine path — high-priority, exercises rules code |
| `internal/gameengine/counter_threshold.go` | 0.0% | never tested — add at minimum a happy-path test; core engine path — high-priority, exercises rules code |
| `internal/gameengine/damage_replacement.go` | 0.0% | never tested — add at minimum a happy-path test; core engine path — high-priority, exercises rules code |
| `internal/gameengine/keywords_batch.go` | 7.3% | minimal coverage — backfill primary path; core engine path — high-priority, exercises rules code |
| `internal/gameengine/keywords_batch2.go` | 0.0% | never tested — add at minimum a happy-path test; core engine path — high-priority, exercises rules code |
| `internal/gameengine/keywords_batch3.go` | 11.3% | minimal coverage — backfill primary path; core engine path — high-priority, exercises rules code |
| `internal/gameengine/keywords_batch4.go` | 6.3% | minimal coverage — backfill primary path; core engine path — high-priority, exercises rules code |
| `internal/gameengine/keywords_batch5.go` | 0.0% | never tested — add at minimum a happy-path test; core engine path — high-priority, exercises rules code |
| `internal/gameengine/keywords_batch6.go` | 39.2% | below threshold — backfill edge cases; core engine path — high-priority, exercises rules code |
| `internal/gameengine/keywords_companion.go` | 47.5% | below threshold — backfill edge cases; core engine path — high-priority, exercises rules code |
| `internal/gameengine/keywords_flashback_grant.go` | 8.0% | minimal coverage — backfill primary path; core engine path — high-priority, exercises rules code |
| `internal/gameengine/mana_artifacts.go` | 44.7% | below threshold — backfill edge cases; core engine path — high-priority, exercises rules code |
| `internal/gameengine/observer_triggers.go` | 45.5% | below threshold — backfill edge cases; core engine path — high-priority, exercises rules code |
| `internal/gameengine/per_card/aang_and_katara.go` | 6.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/abdel_adrian.go` | 2.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/abigale_poet_laureate.go` | 4.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/abzan_falconer.go` | 7.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/acererak_the_archlich.go` | 5.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/adeline_resplendent_cathar.go` | 47.1% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/adeliz_the_cinder_wind.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/adriana_captain_of_the_guard.go` | 3.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/adrix_and_nev_twincasters.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/aerith_gainsborough.go` | 5.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/aggravated_assault.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/aisha_of_sparks_and_smoke.go` | 3.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ajani_nacatl_pariah.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/akroma_angel_of_fury.go` | 14.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/alania.go` | 1.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/alela_cunning_conqueror.go` | 2.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/alesha_who_smiles_at_death.go` | 2.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/alibou_ancient_witness.go` | 2.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/altair_ibn_la_ahad.go` | 1.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/anafenza_kin_tree_spirit.go` | 3.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/anafenza_the_foremost.go` | 3.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/angel_of_destiny.go` | 4.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/animar.go` | 9.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/anje_falkenrath.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/anowon_the_ruin_thief.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/anticausal_vestige.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/aphelia_viper_whisperer.go` | 1.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/aragorn_the_uniter.go` | 2.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/arahbo_roar_of_the_world.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/archangel_of_tithes.go` | 11.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/archmage_ascension.go` | 47.1% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/archpriest_of_shadows.go` | 3.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/arcum_dagsson.go` | 1.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ardbert_warrior_of_darkness.go` | 1.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/aristocrats.go` | 7.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/arixmethes_slumbering_isle.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/artifact_synergy.go` | 3.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ashling_limitless.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ashling_rekindled.go` | 6.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ashnod_flesh_mechanist.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/atemsis_all_seeing.go` | 5.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/athreos_god_of_passage.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/athreos_shroud_veiled.go` | 39.7% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/atraxa_grand.go` | 2.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/atraxa_praetors.go` | 2.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/auntie_ool.go` | 6.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/avatar_aang.go` | 33.3% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ayara_first_of_locthwain.go` | 3.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ayesha_tanaka_armorer.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/azlask_swelling_scourge.go` | 5.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/balmor_battlemage_captain.go` | 3.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/baral_and_kari_zev.go` | 5.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/batch17_sweep.go` | 21.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/belakor_dark_master.go` | 4.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/berta_wise_extrapolator.go` | 4.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/betor_ancestors_voice.go` | 3.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/betor_kin_to_all.go` | 2.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/bhaal_lord_of_murder.go` | 3.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/birthing_ritual.go` | 1.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/bladewing.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/bladewing_deathless_tyrant.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/blech.go` | 3.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/blim_comedic_genius.go` | 9.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/bloodchief_ascension.go` | 4.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/bloodthirsty_conqueror.go` | 7.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/brago_king_eternal.go` | 2.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/braids_arisen_nightmare.go` | 1.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/bre_of_clan_stoutarm.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/breaching_leviathan.go` | 3.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/breena.go` | 1.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/bria_riptide_rogue.go` | 4.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/bringer_of_the_last_gift.go` | 2.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/bruce_banner.go` | 8.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/brudiclad_telchor_engineer.go` | 1.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/bruse_tarl_boorish_herder.go` | 6.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/bugenhagen_wise_elder.go` | 6.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/bumi_unleashed.go` | 2.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/burakos_party_leader.go` | 2.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/bygone_bishop.go` | 6.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/caesar_legions_emperor.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/cait_cage_brawler.go` | 2.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/captain_howler_sea_scourge.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/carmen_cruel_skymarcher.go` | 5.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/celestial_unicorn.go` | 7.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/chainer_nightmare_adept.go` | 8.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/chains_of_mephistopheles.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/chandra_fire_of_kaladesh.go` | 33.3% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/chaos_cascade.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/child_of_alara.go` | 4.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/chishiro_the_shattered_blade.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/claim_jumper.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/clavileno_first_blessed.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/cloud_ex_soldier.go` | 2.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/cloud_planets_champion.go` | 25.0% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/colfenor_the_last_yew.go` | 3.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/combat_restrictions.go` | 25.0% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/commander_staples.go` | 5.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/commanders_batch.go` | 8.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/commodore_guff.go` | 5.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/compy_swarm.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/cormela_glamour_thief.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/crackling_spellslinger.go` | 5.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/craig_boone_novac_guard.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/cruelclaw.go` | 1.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/curious_homunculus.go` | 4.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/custom_amalia_benavides.go` | 44.4% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/custom_choco_seeker_of_paradise.go` | 40.9% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/custom_ghyrson_starn_kelermorph.go` | 47.1% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/custom_saheeli_radiant_creator.go` | 26.7% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/cyclone_summoner.go` | 4.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/daretti_scrap_savant.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/darigaaz_reincarnated.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/dark_leo_and_shredder.go` | 3.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/daxos_blessed_by_the_sun.go` | 28.6% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/defense_grid.go` | 25.0% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/dihada.go` | 0.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/discard_spells.go` | 47.4% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/doomsday_excruciator.go` | 5.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/doran.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/doran_besieged_by_time.go` | 10.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/dr_eggman.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/dr_madison_li.go` | 1.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/drain_commanders.go` | 7.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/dyadrine_synthesis_amalgam.go` | 4.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ed_e_lonesome_eyebot.go` | 5.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/edea_possessed_sorceress.go` | 8.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/edgar_markov.go` | 5.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/edward_kenway.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/elas_il_kor.go` | 6.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/elderscale_wurm.go` | 6.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/elesh_norn_argent_etchings.go` | 19.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/elminster.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/elrond_master_of_healing.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/elsha.go` | 8.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/emet_selch.go` | 11.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/emiel.go` | 1.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/emmara_soul_of_the_accord.go` | 7.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/enchantment_toolbox.go` | 3.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/end_step_intervening_if_family.go` | 6.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/eowyn_shieldmaiden.go` | 5.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/eris_roar_of_the_storm.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/eshki_dragonclaw.go` | 8.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/eshki_temurs_roar.go` | 3.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/etb_tribe_gate_family.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/etrata_deadly_fugitive.go` | 12.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/evercoat_ursine.go` | 9.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/excava_the_risen_past.go` | 2.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ezuri_claw_of_progress.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/fearless_swashbuckler.go` | 1.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/feast_of_the_victorious_dead.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/finneas_ace_archer.go` | 3.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/fire_lord_azula.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/flubs_the_fool.go` | 6.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/food_chain.go` | 48.5% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/francisco.go` | 3.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/frodo_adventurous_hobbit.go` | 3.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/frodo_saurons_bane.go` | 5.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/game_changers.go` | 21.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/garth_one_eye.go` | 4.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gated_etb_effect_family.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gau_feral_youth.go` | 5.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_alpharael_stonechosen.go` | 20.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_bilbo_birthday_celebrant.go` | 47.7% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_cecil_dark_knight_cecil_redeemed_paladin.go` | 20.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_dargo_the_shipwrecker.go` | 16.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_exava_rakdos_blood_witch.go` | 10.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_firesong_and_sunspeaker.go` | 23.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_hamza_guardian_of_arashin.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_kuja_genome_sorcerer_trance_kuja_fate_defied.go` | 48.5% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_lluwen_exchange_student_pest_friend.go` | 7.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_lyse_hext.go` | 6.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_mendicant_core_guidelight.go` | 31.0% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_morophon_the_boundless.go` | 2.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_nadu_winged_wisdom.go` | 35.3% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_progenitus.go` | 40.3% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_rakdos_lord_of_riots.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_rienne_angel_of_rebirth.go` | 2.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_samut_the_driving_force.go` | 37.5% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_sandman_shifting_scoundrel.go` | 10.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_sauron_the_necromancer.go` | 2.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_storm_force_of_nature.go` | 42.3% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_strider_ranger_of_the_north.go` | 3.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_tajic_blade_of_the_legion.go` | 5.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_tam_observant_sequencer_deep_sight.go` | 8.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_the_master_transcendent.go` | 2.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_the_reality_chip.go` | 44.6% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_toph_greatest_earthbender.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gen_ty_lee_chi_blocker.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/genesis_chamber.go` | 5.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/geological_appraiser.go` | 8.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ghave_guru_of_spores.go` | 2.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gisa_the_hellraiser.go` | 5.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gisela_the_broken_blade.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gishath_suns_avatar.go` | 2.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gitrog_ravenous_ride.go` | 1.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gix_yawgmoth_praetor.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/glarb_calamitys_augur.go` | 5.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/glissa_sunslayer.go` | 5.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/go_shintai_lifes_origin.go` | 8.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/goblin_goliath.go` | 6.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/golbez.go` | 3.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/golos_tireless_pilgrim.go` | 5.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gonti_canny_acquisitor.go` | 3.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gorma.go` | 5.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gornog_the_red_reaper.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/goro_goro_satoru.go` | 8.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/grave_scrabbler.go` | 2.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/grave_venerations.go` | 6.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/great_hall_of_the_biblioplex.go` | 47.2% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/greven_predator_captain.go` | 3.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/grist_the_hunger_tide.go` | 1.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/grub_storied_matriarch.go` | 6.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gruff_triplets.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/grunn_the_lonely_king.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gwen_stacy_ghost_spider.go` | 11.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/gwenom_remorseless.go` | 12.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/hakbal.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/halana_and_alena_partners.go` | 3.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/hashaton.go` | 2.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/hazel_of_the_rootbloom.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/hearthhull.go` | 6.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/helga_skittish_seer.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/heliod_sun_crowned.go` | 39.0% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/henry_wu_ingen_geneticist.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/henzie_toolbox_torre.go` | 3.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/herigast_erupting_nullkite.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/hermes_overseer_of_elpis.go` | 8.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/hinata_dawn_crowned.go` | 16.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/hope_estheim.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/hurkyl_master_wizard.go` | 2.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/hylda_of_the_icy_crown.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ichorid.go` | 14.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ikra_shidiqi.go` | 3.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/illuna_apex_of_wishes.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/iron_spider_stark_upgrade.go` | 11.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/isamaru_hound_of_konda.go` | 25.0% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/it_that_betrays.go` | 5.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/jacob_frye.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/jarad_golgari_lich_lord.go` | 4.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/jaws_relentless_predator.go` | 4.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/jeleva_nephalias_scourge.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/jenova_ancient_calamity.go` | 2.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/jenson_carthalion_druid_exile.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/jhoira_weatherlight_captain.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/jin_gitaxias_progress_tyrant.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/jin_sakai_ghost_of_tsushima.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/jinnie_fay.go` | 25.0% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/jodah_the_unifier.go` | 3.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/joshua_phoenixs_dominant.go` | 43.6% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/k_9_mark_i.go` | 18.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kaalia.go` | 2.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kadena_slinking_sorcerer.go` | 8.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kaito_shizuki.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kami_of_transience.go` | 7.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kamiz_obscura_oculus.go` | 12.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/karlach_fury_of_avernus.go` | 3.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/karn_the_great_creator.go` | 48.4% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/katara_waterbending_master.go` | 5.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kazuul_tyrant_of_the_cliffs.go` | 5.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kediss_emberclaw_familiar.go` | 3.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kefka.go` | 8.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kellan_the_kid.go` | 12.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kenrith_returned_king.go` | 1.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ketramose_the_new_dawn.go` | 5.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kibo_uktabi_prince.go` | 3.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/killian_decisive_mentor.go` | 2.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/killian_ink_duelist.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kilo_apogee_mind.go` | 5.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kirol_attentive_first_year.go` | 12.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kirri_talented_sprout.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/knight_of_the_white_orchid.go` | 2.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kodama_of_the_east_tree.go` | 2.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/koma_cosmos_serpent.go` | 18.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/korvold_fae_cursed_king.go` | 4.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kraum.go` | 7.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kraum_violent_cacophony.go` | 7.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/krenko.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kros_defense_contractor.go` | 6.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/krrik_son_of_yawgmoth.go` | 8.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kumena_tyrant_of_orazca.go` | 2.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kykar_winds_fury.go` | 3.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kylox_visionary_inventor.go` | 3.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/kynaios_and_tiro_of_meletis.go` | 3.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/land_tax.go` | 15.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/lasting_tarfire.go` | 4.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/lathiel_the_bounteous_dawn.go` | 5.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/lathril_blade_of_the_elves.go` | 3.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/laughing_jasper_flint.go` | 2.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/leonardo_the_balance.go` | 4.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/leori_sparktouched_hunter.go` | 9.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/life_of_the_party.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/lifegain_counter_family.go` | 7.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/lifegain_endstep_family.go` | 4.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/lilah_undefeated_slickshot.go` | 12.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/lonis_cryptozoologist.go` | 38.5% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/loot_the_pathfinder.go` | 2.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/lord_jyscal_guado.go` | 4.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/lord_windgrace.go` | 1.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/lorehold.go` | 8.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/lorehold_archivist.go` | 14.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/lux_artillery.go` | 3.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/magda_brazen_outlaw.go` | 11.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/magda_the_hoardmaster.go` | 5.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/magus_lucea_kane.go` | 5.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/mahadi_emporium_master.go` | 6.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/malcolm.go` | 4.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/mana_crypt.go` | 42.4% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/marcus_mutant_mayor.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/markov_purifier.go` | 4.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/marrow_gnawer.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/marwyn_the_nurturer.go` | 5.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/mary_read_and_anne_bonny.go` | 4.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/mass_of_mysteries.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/mazirek_kraul_death_priest.go` | 5.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/megatron_tyrant.go` | 26.7% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/meren.go` | 3.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/meria.go` | 2.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/mica.go` | 1.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/miirym_sentinel_wyrm.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/millicent_restless_revenant.go` | 3.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/mindslaver.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/minn_wily_illusionist.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/minthara_merciless_soul.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/mishra_claimed_by_gix.go` | 3.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/mizzix_of_the_izmagnus.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/momo_playful_pet.go` | 3.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/morcant.go` | 7.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/moseo_veins_new_dean.go` | 5.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/mox_diamond.go` | 3.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/mr_foxglove.go` | 2.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/mr_house_president_and_ceo.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ms_bumbleflower.go` | 2.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/muddle_ever_changing.go` | 11.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/muldrotha_the_gravetide.go` | 5.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/myrel_shield_of_argive.go` | 4.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/najeela_blade_blossom.go` | 2.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/namor_sub_mariner.go` | 44.4% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/narset_enlightened_master.go` | 2.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/nashi_moon_sages_scion.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/necromancy.go` | 19.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/necrotic_ooze.go` | 22.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/neheb_the_worthy.go` | 6.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/nekusar.go` | 7.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/nelly_borca_impulsive_accuser.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/nessian_wilds_ravager.go` | 3.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/nicol_bolas.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/nine_fingers_keene.go` | 2.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/nita_forum_conciliator.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/niv_mizzet_firemind.go` | 6.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/niv_mizzet_parun.go` | 8.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/niv_mizzet_reborn.go` | 2.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/niv_mizzet_visionary.go` | 9.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/notion_thief.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ob_nixilis_captive.go` | 3.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/obeka_support.go` | 11.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ohabi_caleria.go` | 1.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ojer_axonil.go` | 6.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/olivia_opulent_outlaw.go` | 2.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/omo_queen_of_vesuva.go` | 5.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/oracle_of_bones.go` | 2.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ormacar_relic_wraith.go` | 25.0% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/orvar_all_form.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/osgir_the_reconstructor.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/otharri_suns_glory.go` | 4.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/oversold_cemetery.go` | 3.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ovika_enigma_goliath.go` | 5.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/pact_of_negation.go` | 37.7% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/pako_arcane_retriever.go` | 4.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/panglacial_wurm.go` | 16.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/pantlaza.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/paradigm_decorum_dissertation.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/paradigm_echocasting_symposium.go` | 18.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/paradigm_germination_practicum.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/paradigm_improvisation_capstone.go` | 3.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/paradigm_restoration_seminar.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/pernicious_deed.go` | 43.5% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/perplexing_chimera.go` | 3.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/phoenix_fleet_airship.go` | 36.8% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/port_razer.go` | 3.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/preston_the_vanisher.go` | 2.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/primo_the_unbounded.go` | 3.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/prismari.go` | 5.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/prosper_tome_bound.go` | 5.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/purphoros_god_of_the_forge.go` | 5.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/queza.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/quicksilver_fountain.go` | 3.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/quilled_greatwurm.go` | 7.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/quintorius.go` | 2.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/radagast_wizard_of_wilds.go` | 2.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/radha_heir_to_keld.go` | 7.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/raff_weatherlight_stalwart.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/raffine.go` | 2.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ragost_combo.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ral_monsoon_mage.go` | 33.3% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rankle_and_torbran.go` | 4.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/raphael_fiendish_savior.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rashmi_and_ragavan.go` | 3.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rasputin_dreamweaver.go` | 6.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ratadrabik_of_urborg.go` | 5.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ravenloft_adventurer.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rex_cyber_hound.go` | 7.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ria_ivor_bane_of_bladehold.go` | 3.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rielle_the_everwise.go` | 7.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/riku_of_two_reflections.go` | 3.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rin_and_seri_inseparable.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rith_the_awakener.go` | 2.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rivaz_of_the_claw.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/river_songs_diary.go` | 4.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rocco_cabaretti_caterer.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rocco_street_chef.go` | 6.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rograkh.go` | 20.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/roil_elemental.go` | 2.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rootha.go` | 2.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rootwater_matriarch.go` | 2.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rosa_resolute_white_mage.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rowan_scion.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/roxanne_starfall_savant.go` | 6.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ruin_lurker_bat.go` | 6.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/rune_scarred_demon.go` | 3.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/runo_stromkirk.go` | 6.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sam_loyal_attendant.go` | 9.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sand_scout.go` | 6.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sanguine_exquisite.go` | 3.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sarcomancy.go` | 7.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/saruman_of_many_colors.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/saruman_white_hand.go` | 9.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/satoru_umezawa.go` | 1.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/satya.go` | 1.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sauron_dark_lord.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sauron_lord_of_the_rings.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/savra_queen_of_the_golgari.go` | 2.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/scion_of_the_ur_dragon.go` | 6.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/scorpion_seething_striker.go` | 3.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/scriv_the_obligator.go` | 4.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sefris_of_the_hidden_ways.go` | 3.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/selvala.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sephiroth.go` | 7.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/shalai_and_hallar.go` | 6.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/shanna_purifying_blade.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sheoldred_the_apocalypse.go` | 5.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sheoldred_true_scriptures.go` | 28.4% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/shiko.go` | 1.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/shirei_shizos_caretaker.go` | 4.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/shroofus_sproutsire.go` | 3.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sidar_jabari_of_zhalfir.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sidar_kondo.go` | 14.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sidisi_brood_tyrant.go` | 7.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sin_spiras_punishment.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sisay.go` | 2.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/skithiryx_the_blight_dragon.go` | 2.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/skitterbeam_battalion.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/slicer_hired_muscle.go` | 40.0% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/slimefoot_and_squee.go` | 10.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/slimefoot_the_stowaway.go` | 6.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/smeagol_helpful_guide.go` | 3.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/smellerbee_rebel_fighter.go` | 3.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/smirking_spelljacker.go` | 5.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sorin_of_house_markov.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sovereign_okinec_ahau.go` | 5.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/squall_gunblade_duelist.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/squall_seed_mercenary.go` | 3.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/star_charter.go` | 1.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/stax_spells.go` | 4.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/stella_lee_wild_card.go` | 7.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/strefan_maurer_progenitor.go` | 15.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/strionic_resonator.go` | 3.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sun_spider_nimble_webber.go` | 3.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sunderflock.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/svella_ice_shaper.go` | 2.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sword_feast_famine.go` | 3.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sygg_river_cutthroat.go` | 6.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sygg_wanderwine_wisdom.go` | 6.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/syr_gwyn_hero_of_ashvale.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/syr_vondam_sunstar_exemplar.go` | 7.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/syr_vondam_the_lucent.go` | 7.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/sythis_harvests_hand.go` | 6.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/szarel_genesis_shepherd.go` | 3.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/taii_wakeen_perfect_shot.go` | 8.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/talion_the_kindly_lord.go` | 5.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/tasha_the_witch_queen.go` | 10.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/tayam.go` | 5.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/teferi_temporal_archmage.go` | 25.0% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/tellah_great_sage.go` | 3.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/temmet_naktamuns_will.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/terra.go` | 43.3% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/terra_herald_of_hope.go` | 3.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/teval.go` | 2.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/tevesh_szat.go` | 1.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/teysa_karlov.go` | 5.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/teysa_orzhov_scion.go` | 7.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/thalia_and_the_gitrog_monster.go` | 3.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/thantis_the_warweaver.go` | 7.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/the_ancient_one.go` | 40.9% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/the_archimandrite.go` | 5.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/the_council_of_four.go` | 8.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/the_ever_changing_dane.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/the_first_sliver.go` | 5.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/the_ghoul_gunslinger.go` | 16.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/the_gitrog_monster.go` | 3.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/the_mimeoplasm.go` | 2.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/the_necrobloom.go` | 7.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/the_rani.go` | 5.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/the_ur_dragon.go` | 1.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/the_war_doctor.go` | 7.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/the_watcher_in_the_water.go` | 6.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/the_wise_mothman.go` | 7.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/thrakkus_the_butcher.go` | 3.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/thrasios.go` | 5.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/tiamat.go` | 2.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/tidus_yunas_guardian.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/tifa_martial_artist.go` | 3.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/titania_voice_of_gaea.go` | 5.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/tivash_gloom_summoner.go` | 3.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/tivit.go` | 8.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/token_triggers.go` | 6.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/tolsimir_midnights_light.go` | 12.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/tombstone_stairwell.go` | 6.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/torment_of_hailfire.go` | 2.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/tovolar_dire_overlord.go` | 10.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/transcendent_dragon.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/treasure_nabber.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/treva_the_renewer.go` | 3.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/tribal_lords.go` | 18.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/trumpeting_carnosaur.go` | 7.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/twilight_prophet.go` | 6.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/tymna.go` | 5.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ulalek.go` | 2.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/umbris_fear_manifest.go` | 2.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/underworld_breach.go` | 29.1% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/unstable_glyphbridge.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/urabrask_the_great_work.go` | 40.9% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ureni_of_the_unwritten.go` | 3.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/urtet_remnant_of_memnarch.go` | 7.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/urza_chief_artificer.go` | 5.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/urza_lord_high_artificer.go` | 44.2% | below threshold — backfill edge cases; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/utility_lands.go` | 22.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/valakut_exploration.go` | 4.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/valgavoth_harrower_of_souls.go` | 3.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/valki_god_of_lies.go` | 12.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/vannifar_evolved_enigma.go` | 2.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/varolz_the_scar_striped.go` | 2.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/vazi_keen_negotiator.go` | 3.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/vecna_trilogy.go` | 5.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/venser_corpse_puppet.go` | 2.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/vial_smasher.go` | 3.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/vibrance.go` | 2.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/viconia_drow_apostate.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/vihaan_goldwaker.go` | 2.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/vincent_valentine.go` | 12.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/vito_thorn_of_the_dusk_rose.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/volatile_stormdrake.go` | 1.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/vraska_the_silencer.go` | 5.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/vren.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/vrondiss_rage_of_ancients.go` | 12.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/wary_farmer.go` | 4.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/wedding_ring.go` | 3.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/wilhelt_the_rotcleaver.go` | 2.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/winota_joiner_of_forces.go` | 1.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/winter_cynical_opportunist.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/winter_misanthropic_guide.go` | 7.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/witch_of_the_moors.go` | 2.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/witherbloom.go` | 10.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/worldspine_wurm.go` | 4.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/wort_boggart_auntie.go` | 4.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/wyleth_soul_of_steel.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/xenagos_god_of_revels.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/xu_ifit_osteoharmonist.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/xyris_the_writhing_storm.go` | 9.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/yathan_roadwatcher.go` | 2.9% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/yawgmoth_thran_physician.go` | 4.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/ygra_eater_of_all.go` | 8.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/yidris_maelstrom_wielder.go` | 5.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/yorvo_lord_of_garenbrig.go` | 6.1% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/yoshimaru_ever_faithful.go` | 6.7% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/yshtola_nights_blessed.go` | 3.5% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/yuna_grand_summoner.go` | 4.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/yuriko.go` | 2.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/yusri_fortunes_flame.go` | 6.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/zada_hedron_grinder.go` | 2.4% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/zask_skittering_swarmlord.go` | 2.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/zaxara_the_exemplary.go` | 4.3% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/zeriam_golden_wind.go` | 4.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/zimone_and_dina.go` | 10.8% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/zimone_infinite_analyst.go` | 4.0% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/zinnia_valleys_voice.go` | 5.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/zndrsplt_eye_of_wisdom.go` | 6.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/zoraline_cosmos_caller.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/zoyowa_lava_tongue.go` | 5.6% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/zurgo_ojutai.go` | 4.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/zurgo_stormrender.go` | 3.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/per_card/zurgo_thunders_decree.go` | 6.2% | minimal coverage — backfill primary path; per-card handler — pair with a per_card_*_test.go regression in the same dir |
| `internal/gameengine/play_from_graveyard.go` | 46.0% | below threshold — backfill edge cases; core engine path — high-priority, exercises rules code |
| `internal/gameengine/reflexive_triggers.go` | 0.0% | never tested — add at minimum a happy-path test; core engine path — high-priority, exercises rules code |
| `internal/gameengine/resolve_helpers.go` | 19.3% | minimal coverage — backfill primary path; core engine path — high-priority, exercises rules code |
| `internal/gameengine/scaling.go` | 15.7% | minimal coverage — backfill primary path; core engine path — high-priority, exercises rules code |
| `internal/gameengine/targets.go` | 36.1% | below threshold — backfill edge cases; core engine path — high-priority, exercises rules code |
| `internal/gameengine/tokens.go` | 46.8% | below threshold — backfill edge cases; core engine path — high-priority, exercises rules code |
| `internal/gameengine/zone_cast_policy.go` | 19.0% | minimal coverage — backfill primary path; core engine path — high-priority, exercises rules code |
| `internal/hat/gameplan.go` | 10.3% | minimal coverage — backfill primary path; AI decision logic — add a focused MCTS / heuristic test |
| `internal/hat/octo.go` | 0.0% | never tested — add at minimum a happy-path test; AI decision logic — add a focused MCTS / heuristic test |
| `internal/hat/poker.go` | 49.4% | below threshold — backfill edge cases; AI decision logic — add a focused MCTS / heuristic test |
| `internal/hat/selfplay.go` | 0.0% | never tested — add at minimum a happy-path test; AI decision logic — add a focused MCTS / heuristic test |
| `internal/hat/strategy_loader.go` | 45.8% | below threshold — backfill edge cases; AI decision logic — add a focused MCTS / heuristic test |
| `internal/heimdall/contract_replay.go` | 5.0% | minimal coverage — backfill primary path; internal utility — add unit tests for the public surface |
| `internal/heimdall/replay.go` | 39.8% | below threshold — backfill edge cases; internal utility — add unit tests for the public surface |
| `internal/heimdall/seed_binary.go` | 0.0% | never tested — add at minimum a happy-path test; internal utility — add unit tests for the public surface |
| `internal/hexapi/cards.go` | 27.2% | below threshold — backfill edge cases; HTTP/WS handler — add an endpoint integration test |
| `internal/hexapi/cardstats.go` | 4.6% | minimal coverage — backfill primary path; HTTP/WS handler — add an endpoint integration test |
| `internal/hexapi/contrib.go` | 35.1% | below threshold — backfill edge cases; HTTP/WS handler — add an endpoint integration test |
| `internal/hexapi/contrib_spotcheck.go` | 0.0% | never tested — add at minimum a happy-path test; HTTP/WS handler — add an endpoint integration test |
| `internal/hexapi/handler.go` | 49.1% | below threshold — backfill edge cases; HTTP/WS handler — add an endpoint integration test |
| `internal/hexapi/huginn_adapter.go` | 0.0% | never tested — add at minimum a happy-path test; HTTP/WS handler — add an endpoint integration test |
| `internal/hexapi/meta_trends.go` | 46.7% | below threshold — backfill edge cases; HTTP/WS handler — add an endpoint integration test |
| `internal/hexapi/muninn_adapter.go` | 0.0% | never tested — add at minimum a happy-path test; HTTP/WS handler — add an endpoint integration test |
| `internal/hexapi/openapi_validate.go` | 31.2% | below threshold — backfill edge cases; HTTP/WS handler — add an endpoint integration test |
| `internal/hexapi/search.go` | 34.4% | below threshold — backfill edge cases; HTTP/WS handler — add an endpoint integration test |
| `internal/hexapi/showmatch.go` | 22.9% | minimal coverage — backfill primary path; HTTP/WS handler — add an endpoint integration test |
| `internal/hexapi/spectate_rooms.go` | 11.6% | minimal coverage — backfill primary path; HTTP/WS handler — add an endpoint integration test |
| `internal/hexapi/telemetry_adapter.go` | 0.0% | never tested — add at minimum a happy-path test; HTTP/WS handler — add an endpoint integration test |
| `internal/hexapi/versioning.go` | 0.0% | never tested — add at minimum a happy-path test; HTTP/WS handler — add an endpoint integration test |
| `internal/huginn/huginn.go` | 41.7% | below threshold — backfill edge cases; internal utility — add unit tests for the public surface |
| `internal/moxfield/parser.go` | 0.0% | never tested — add at minimum a happy-path test; internal utility — add unit tests for the public surface |
| `internal/moxfield/resolver.go` | 0.0% | never tested — add at minimum a happy-path test; internal utility — add unit tests for the public surface |
| `internal/paritycheck/paritycheck.go` | 35.7% | below threshold — backfill edge cases; internal utility — add unit tests for the public surface |
| `internal/party/handler.go` | 0.0% | never tested — add at minimum a happy-path test; internal utility — add unit tests for the public surface |
| `internal/telemetry/ga4.go` | 0.0% | never tested — add at minimum a happy-path test; internal utility — add unit tests for the public surface |
| `internal/tournament/audit.go` | 0.0% | never tested — add at minimum a happy-path test; tournament runner — add a small-N pool simulation test |
| `internal/tournament/balanced_pool.go` | 24.5% | minimal coverage — backfill primary path; tournament runner — add a small-N pool simulation test |
| `internal/tournament/chunk.go` | 0.0% | never tested — add at minimum a happy-path test; tournament runner — add a small-N pool simulation test |
| `internal/tournament/double_elim.go` | 23.2% | minimal coverage — backfill primary path; tournament runner — add a small-N pool simulation test |
| `internal/tournament/report.go` | 38.3% | below threshold — backfill edge cases; tournament runner — add a small-N pool simulation test |
| `internal/tournament/roundrobin.go` | 0.0% | never tested — add at minimum a happy-path test; tournament runner — add a small-N pool simulation test |
| `internal/tournament/runner.go` | 46.2% | below threshold — backfill edge cases; tournament runner — add a small-N pool simulation test |
| `internal/tournament/swiss.go` | 25.6% | below threshold — backfill edge cases; tournament runner — add a small-N pool simulation test |
| `internal/ws/handler.go` | 0.0% | never tested — add at minimum a happy-path test; internal utility — add unit tests for the public surface |
| `scripts/find_high_freq_uncovered/main.go` | 0.0% | never tested — add at minimum a happy-path test |
