package per_card

// registerAsmoranomardicadaistinaculdacar is the auto-generated entry
// point retained so the batch_generated.go registry call keeps
// compiling. The real ETB (tutor The Underworld Cookbook from library
// to hand) plus the {sac 2 Foods}: 6 damage to target creature
// activated ability live in custom_asmoranomardicadaistinaculdacar.go
// and are wired via registerAsmoranomardicadaistinaculdacarCustom
// (registered from registry.go).
//
// The auto-gen body emitted TWO misleading partial breadcrumbs on every
// Asmor ETB even though the custom handler already implemented both
// the tutor and the activated ability. Neutered here (R49 batch C)
// so the partial channel doesn't surface duplicate parser-gap signals.
func registerAsmoranomardicadaistinaculdacar(r *Registry) {
	_ = r
}
