# CLAUDE.md — HexDek

## What is HexDek

HexDek is an open-source MTG (Magic: The Gathering) Commander game engine, AI player, and analysis platform. It simulates 4-player Commander games with AI-driven decision-making, deck analysis, and tournament systems.

- **License:** MIT
- **Language:** Go (engine, AI, tools), React/TypeScript (frontend)
- **Module:** `github.com/hexdek/hexdek`

## Architecture

```
cmd/                    # Executable entry points
  hexdek-server/      # Main API server (WebSocket game engine + REST API)
  hexdek-freya/       # Deck analysis pipeline (archetype, combos, win lines, roles)
  hexdek-heimdall/    # Post-game analytics + spectator tool
  hexdek-thor/        # Card corpus parser (Scryfall → AST)
  hexdek-import/      # Moxfield deck importer
  hexdek-tournament/  # CLI tournament runner
  hexdek-loki/        # Fuzz tester (random games, crash detection)
  hexdek-judge/       # Rules compliance checker
  hexdek-valkyrie/    # Deck effectiveness ranker
  hexdek-odin/        # Oracle text analyzer
  hexdek-parity/      # Cross-engine parity checker
  dump_drift/           # ELO drift reporter

internal/               # Core packages
  gameengine/           # Rules engine (zones, combat, triggers, SBAs, stack)
  gameast/              # Card AST (parsed oracle text → structured abilities)
  hat/                  # AI player ("YggdrasilHat" — MCTS + heuristics + Freya intelligence)
  analytics/            # Heimdall post-game analytics (card rankings, missed combos, stall detection)
  tournament/           # Tournament runner (round-robin, pool, gauntlet)
  hexapi/               # HTTP/WS API layer
  hub/                  # WebSocket hub for live spectator
  party/                # Game lobby / party system
  auth/                 # Authentication
  db/                   # SQLite persistence (ELO, game history)
  deckparser/           # Deck list parser (text → cards)
  moxfield/             # Moxfield API client
  oracle/               # Scryfall oracle corpus loader
  astload/              # AST dataset loader
  rules/                # Comprehensive Rules text parser
  mana/                 # Mana cost/pool algebra
  shuffle/              # Fisher-Yates shuffle
  trueskill/            # TrueSkill rating system
  ai/                   # LLM integration (Ollama)
  deckid/               # Deck identity hashing
  paritycheck/          # Cross-engine parity utilities
  game/                 # Game state serialization
  ws/                   # WebSocket utilities

hexdek/                 # Frontend (React + Vite)
  src/
    screens/            # Spectator, Decks, Queue, Party screens
    components/         # UI components
  public/

data/
  decks/                # Deck lists (text files)
  rules/                # Scryfall oracle data, AST datasets (gitignored, large)
```

## Tool Suite (Norse Mythology Naming)

| Tool | Purpose | Key Flags |
|------|---------|-----------|
| **Thor** | Parses Scryfall oracle corpus → AST dataset | `--oracle`, `--output` |
| **Odin** | Oracle text analysis and search | `--search`, `--pattern` |
| **Freya** | Deck analysis: archetype, combos, roles, win lines, bracket | `--deck`, `--json`, `--strategy` |
| **Loki** | Fuzz testing: random games looking for crashes | `--games`, `--timeout` |
| **Heimdall** | Post-game analytics + spectator | `--replay`, `--stats` |
| **Valkyrie** | Deck effectiveness ranking via tournament | `--decks`, `--games` |

## AI Architecture: YggdrasilHat

The AI player uses a layered evaluation system:

1. **8 Evaluation Dimensions:** BoardPresence, CardAdvantage, ManaAdvantage, LifeResource, ComboProximity, ThreatExposure, CommanderProgress, GraveyardValue
2. **Archetype-Aware Weights:** Freya provides per-deck MCTS weights (e.g., Combo decks boost ComboProximity to 2.0)
3. **Strategy Profile Bridge:** Freya's analysis (combos, roles, finishers, color demand, value engines) feeds directly into hat decisions
4. **3rd Eye Intelligence:** Opponent tracking (cards seen, perceived archetype, threat assessment)
5. **Budget System:** 0=heuristic, 1-199=evaluator-guided, 200+=rollout. Adaptive degradation on complex boards.
6. **Conviction System:** Concession detection based on relative position tracking

## Common Commands

```bash
# Build everything
go build ./...

# Run tests
go test ./internal/gameengine/... -count=1 -timeout 300s
go test ./internal/hat/... -count=1
go test ./internal/tournament/... -count=1

# Run the server (needs oracle-cards.json in data/rules/)
go run ./cmd/hexdek-server/

# Analyze a deck
go run ./cmd/hexdek-freya/ --deck data/decks/mydeck.txt

# Run a tournament
go run ./cmd/hexdek-tournament/ --decks data/decks/ --games 100

# Cross-compile for DARKSTAR (Linux deployment)
GOOS=linux GOARCH=amd64 go build -o hexdek-server-linux ./cmd/hexdek-server/

# Frontend dev
cd hexdek && npm install && npm run dev

# Frontend build
cd hexdek && VITE_API_URL="" npx vite build
```

## Deployment

- **Engine runs on DARKSTAR** (192.168.1.207:8090) — Ubuntu Linux, Ryzen 9
- **Frontend on MISTY** (192.168.1.200) — behind Caddy at hexdek.dev
- **Deploy script:** `./scripts/deploy.sh [backend|frontend|both]`
- **Manual deploy server:** cross-compile → scp to DARKSTAR → `~/hexdek/start-hexdek.sh` (port 8090)
- **Manual deploy frontend:** `cd hexdek && npm run build` → `rsync --delete hexdek/dist/ josh@...200:~/sites/hexdek/`
- **IMPORTANT:** Frontend source is `hexdek/` (Vite React). Do NOT deploy from any other directory.
- Requires WireGuard VPN when remote: `sudo wg-quick up ~/.config/wireguard/admin-vpn.conf`

## Data Files

Large data files are gitignored and must be fetched locally:
- `data/rules/oracle-cards.json` (163MB) — Scryfall bulk oracle data
- `data/rules/ast_dataset.jsonl` (46MB) — Thor's parsed AST output
- `data/rules/finetune_pairs.jsonl` (56MB) — Training data

Fetch oracle data: `scripts/fetch-oracle.sh`

## Freya Improvement Kanban

### Ready

### Done (2026-05-25)
- [x] **Loki r60 canonical-final** (PR #427) — 10K games × 10 canonical seeds = **0 violations / 0 crashes / 0 panics** across 100K chaos + 100K nightmare. ENGINE OFFICIALLY CLEAN at canonical-seed scale. Per-game stochastic rate: r41 33% → r60 0% (−100%). Full verdict in `docs/loki-r60-canonical-final.md`. The r60 era is closed. 12 distinct lifecycle/invariant/per_card fixes shipped during the stress-discovery cycle (see Issue Log Resolved table for the full audit trail PR #169 / #178 / #184 / #190 / #200 / #201 / #286 / #399 / #402 / #407 / commit 9f801ed / #419).
- [x] Composition-prior into TrueSkill — multi-PR feature wave, takes deck-vs-deck matchup matrix from Freya and folds it into rating updates as a prior. **(PR #403)** implementation: `CompositionPrior` struct + `CompositionScore` helper that maps an archetype tuple to an expected-winrate vector via Freya's matchup matrix; **(PR #408)** wired into `TrueSkillRatings.Update` path as a pre-game expectation that shrinks the mu delta when the matchup-implied result was already predicted, and amplifies it when surprising; **(PR #411)** validation gauntlet at +1.4pp accuracy / +0.036 log-loss vs the no-prior baseline (`docs/composition-prior-validation.md`); **(PR #415)** wired into live showmatch updateELO; **(PR #420)** Heimdall analytics surfaces the per-game composition-effect contribution; **(PR #424)** new `hexdek-composition-replay` debug CLI for offline what-if analysis; **(PR #428)** Wilson 95% confidence intervals on the per-matchup prior cell so low-data archetypes don't overweight the rating delta.
- [x] System-assigned archetype tags from Freya **(PR #416)** — Freya's archetype classification now flows into the Decks app as a system-assigned tag per deck. Surfaces in deck list views, filter dropdowns, and the deck-detail header. Distinguishes from user-assigned tags (different render color) so the source is obvious.
- [x] Archetype confirm/correct UI + training log **(PR #421)** — deck owner can confirm Freya's archetype call OR correct it to a different tag; corrections write to a training-log endpoint that feeds the next Freya retune wave. Closes the loop from "Freya guesses" → "user corrects" → "Freya learns."
- [x] Archetype change history across deck versions **(PR #425)** — tracks how Freya's classification shifted as the deckbuilder iterated. Useful for spotting "this deck became its archetype on revision N" stories + for deckbuilders to know when their changes actually shifted the classifier's verdict.
- [x] Training-signal endpoint for archetype-correction clusters **(PR #429)** — aggregates the #421 user corrections into clusters (deck-similarity buckets within an incorrect-archetype group); surfaces the top mis-classified clusters for the next Freya wave to prioritize.
- [x] Saved deck views **(PR #404)** — per-user saved filter/sort presets on the Decks screen ("My commander deck", "B4 jank pile", etc.). Used by the archetype-correction workflow to focus on specific subsets.
- [x] Recent games view on Decks **(PR #266)** — surfaces last-N games per deck inline with the deck row; clicking jumps to the Heimdall summary. Companion data feed for the composition-prior monitoring.
- [x] Hat self-trigger response work — multi-PR wave. **(PR #410)** `ChooseResponse` counters own trigger when self-harm is lethal (e.g. own Manabarbs would burn lethal damage); **(PR #413)** extended the matrix to mill + life-loss lethal scenarios; **(PR #418)** full self-trigger response matrix suite covering 12 trigger-categories × 4 lethal-state classes; **(PR #422)** validation gauntlet at 2,500 games × 5 seeds, 0 self-harm fires in any tested scenario (`docs/hat-self-trigger-gauntlet-r60.md`); **(PR #423)** hat self-trigger validation companion test fixture; **(PR #426)** continued validation work.
- [x] Hat ChooseAttackers fold attack-trigger value **(PR #339)** — per-attacker score now factors in expected attack-trigger value (Edric/Bygone draws, Surveil-on-attack, treasure-on-attack, etc.) so the hat picks attack lineups that maximize trigger-stack EV vs raw P/T.
- [x] Hat cardHeuristic ETB-trigger value bonus **(PR #406)** — cast prioritization now folds ETB-trigger value so creatures with strong ETBs (Mulldrifter, Reclamation Sage, etc.) get cast over equal-cost creatures without.
- [x] Hat ChooseResponse counter-own-trigger sibling **(PR #405)** — target-priority followup to PR #410 covering edge cases where the counter is correct EV even when not strictly lethal.
- [x] Hat priority self-harm bridge **(PR #412, PR #417)** — finer-grained stack-trigger handling for the self-harm response chain.
- [x] Pet card detection — identify low-tier creatures the deckbuilder kept despite an off-archetype fit (signals personal taste outweighs optimization, so the upgrade coaching shouldn't hammer "cut this"). New `computePetCards` in `cmd/hexdek-freya/advanced.go` runs after `computeCardQualityTiers`. Detection requires ALL of: (1) PowerTier in {C, D} — low-tier only; (2) TypeLine contains "creature" — players form attachments to creatures, not generic noncreature utility; (3) at least one role tag — pure-filler untagged cards signal "didn't realize this was bad", NOT a pet; (4) NONE of the card's roles match the deck's primary-archetype fingerprint — on-archetype creatures are just low-power role plays, not flavor picks; (5) NOT a CMC 5+ Utility-only dead slot — the existing cuttable path owns those, double-flagging muddies both signals. Legendary creatures get the "signature flavor pick" reason string (legendaries are the strongest pet-card signal — characters players genuinely care about); nonlegendary get "personal-taste pick". Sorted by Power desc, capped at 8 entries. New `PetCard` struct + `DeckProfile.PetCards`; JSON `pet_cards` array; text report renders under "Pet Cards (flavor picks — personal taste outweighs optimization):" with the `♥` glyph and `[tier power]` prefix. Verified on real decks: voja tribal flags 8 off-archetype creatures (Marwyn the Nurturer, Primal Adversary, Anara Wolvid Familiar...); werewolf flags Masked Vandal / Geier Reach Bandit / Reckless Stormseeker; nazgul deck flags the literal Nazgûl creatures themselves (deck classified as Control, so the tribal flavor picks correctly surface as off-archetype keeps). 10 tests in `cmd/hexdek-freya/pet_cards_test.go` (canonical off-archetype creature flagged, on-archetype creature not flagged, noncreature not flagged, high-tier not flagged, untagged not flagged, dead slot not flagged, legendary gets signature framing, capped at 8, power-descending order, partial-role-match not flagged)
- [x] Power-tier threshold recalibration — retuned `PowerTierFor` bands in `cmd/hexdek-freya/deckprofile.go` from 75/60/40/25 to 70/55/38/25 based on the cross-deck distribution data from PR #341. Original bands produced 3.0% S / 14.7% A on the lyon corpus (and 3.3% S / 11.4% A on the 300-deck moxfield corpus) — both elite tiers too thin, with strong-but-not-elite cards in the [70-74] band stuck at A instead of S, and solid supports in the [55-59] band stuck at B instead of A. Lowering S from 75→70 picks up the [70-74] histogram band (~4pp); lowering A from 60→55 picks up [55-59] (~9pp); lowering B from 40→38 lets a couple more cards graduate from C. C/D boundary at 25 left unchanged — D was already well-sized as "obvious cuts". New distribution on lyon corpus: **6.9% S / 19.4% A / 35.5% B / 29.2% C / 8.9% D** (hits target ~7/18/35/30/10 nearly exactly); calibration-corpus run reports 6.3/13.2/44.0/28.8/7.7. Updated boundary tests in `cmd/hexdek-freya/power_tier_test.go` (threshold table + high-inclusive boundary pairs now pin 70/55/38/25). New `cmd/hexdek-freya/power_calibration_test.go` regression test runs the aggregate against `data/decks/test/` and asserts each tier stays within target bands (S 3-15%, A 10-28%, B 25-50%, C 18-40%, D 3-20%, sum-to-1.0 invariant); skips when oracle data absent. Doc comment on `PowerTierFor` now records the recalibration history + 70/55/38/25 rationale
### Done (2026-05-24)
- [x] Power-tier aggregate stats — cross-deck S/A/B/C/D distribution rollup for calibrating the tier thresholds. New `cmd/hexdek-freya/power_aggregate.go` with `ComputePowerTierAggregate(reports) *PowerTierAggregate` and `PrintPowerTierAggregate(w, agg)` — assembles overall tier mix (counts + percentages), summary stats (mean / median / min / max power), per-bracket breakdown (B1-B5 tier %), per-primary-archetype breakdown (sorted by deck count desc), and a 21-bin 5-point score histogram (0-4 through 95-100, top bin inclusive of 100) with inline ASCII bar chart. Hooked into `PrintAllDecksSummary` so any `--all-decks` text run now emits the rollup. New `--power-aggregate-out <path>` flag dumps the structured aggregate as JSON for offline calibration analysis. **Real-world calibration baseline** (300-deck moxfield corpus, 19,673 cards): **3.3% S / 11.4% A / 39.5% B / 37.0% C / 8.8% D** — a healthy distribution peaked at B with thin tails. Notable findings: B5 cEDH decks skew B-heavy (47.7%) not S-heavy, suggesting cEDH builds have many "solid B-tier" supports without wincon/bridge multipliers; Mill and Damage Redirect archetypes have very few S cards (niche); Aggro and Lands Matter skew S-heavy (efficient curves). 9 new tests in `cmd/hexdek-freya/power_aggregate_test.go`: empty-input safety, tier-count+percent identity, mean/median/min/max stats, 21-bin histogram boundaries (incl. top-bin extension to 100), per-bracket grouping + ascending sort, per-archetype deck-count-descending sort, defensive skip of nil/empty profiles, all-sections-present output check, empty-aggregate-renders-nothing
- [x] Power-tier WHY explanations — per-card human-readable rationale for the S/A/B/C/D verdict (builds on PR #308). New `buildPowerExplanation` in `cmd/hexdek-freya/advanced.go` produces a single one-line why-string per card with format `TIER — signal1 + signal2 + signal3` (e.g. `S — wincon piece + 3-role at CMC 2 + Combo fit (Tutor/Combo)`, `D — CMC 6 (curve heavy) + off-archetype + dead slot`). `computeCardPower` now tracks which signals fired for each card (matched archetype-fit roles, multi-low-CMC bonus, wincon/bridge/step/finisher/cluster membership, dead-slot/redundant-tutor penalties) and passes them through a `powerExplanationInputs` struct. The synergy section emits only the SINGLE highest-priority membership (wincon > bridge > finisher > step > cluster) to keep lines scannable. Archetype-fit framing: matched roles render as `Combo fit (Tutor/Combo)`; tagged but unmatched as `off-archetype`; untagged as `untagged`. Penalties always appended at tail so D-tier lines read "why it's D". Defensive fallback so explanation is never empty. New `CardPowerLevel.Explanation` field and `CardQuality.PowerExplanation` (echoed onto each star/solid/cuttable for inline render). JSON adds `explanation` to `card_power_levels` entries and `power_explanation` to each `star_cards`/`solid_cards`/`cuttable_cards` entry. Text report renders the explanation as a second `             why: …` line under each tier-list entry. 8 tests in `cmd/hexdek-freya/power_explain_test.go`: format-shape (leading tier letter, " + " join, all signals present), top-synergy-only priority ordering, off-archetype framing, untagged framing, penalty-at-tail ordering, never-empty defensive fallback, attachment to CardPowerLevels, flow-through to CardQuality. Verified on cedh_muldrotha: `★ [S 84] Thassa's Oracle — wincon piece + 3-role at CMC 2 + Midrange fit (Threat)`, `✂ [D 16] Ashaya — CMC 5 (curve heavy) + off-archetype + dead slot`
- [x] Power-tier S/A/B/C/D grading — builds on the 0-100 per-card power scoring (PR #291) to bucket cards into letter grades for "deck buy-it pacing". New `PowerTierFor(power int) string` in `cmd/hexdek-freya/deckprofile.go` with absolute (NOT percentile) bands: **S** ≥75 (must-keep wincons / load-bearing engines), **A** 60-74 (strong supports), **B** 40-59 (solid utility, replaceable but not flex), **C** 25-39 (situational / role-only filler), **D** 0-24 (cut candidates). Absolute bands are the deliberate design choice — a casual B2 precon correctly reports `0S / 7A / 28B / 17C / 11D` rather than promoting its top filler, conveying "your top card is only A-tier" as the buy-it pacing signal. New `PowerTierOrder` slice for stable iteration (always S→D). `computeCardPower` now stamps `PowerTier` on each `CardPowerLevel` and aggregates `dp.PowerTierCounts` (map pre-seeded with all 5 keys at 0 for stable rendering even when zero cards land in a tier). Each star/solid/cuttable echoes the same `PowerTier` on `CardQuality.PowerTier`. Text report adds header summary `Power Tiers: 2S / 6A / 37B / 22C / 4D (buy S→A first; D = cut candidates)` plus per-line tier prefix `★ [S  84] Thassa's Oracle` / `● [B  40] Demonic Tutor` / `✂ [D  16] Ashaya`. JSON: `power_tier_counts` map + `power_tier` on each tier entry + `power_tier` on each `card_power_levels` entry. 7 tests in `cmd/hexdek-freya/power_tier_test.go` (threshold table, high-inclusive boundary semantics, attachment to CardPowerLevels, histogram sum-to-card-count, all-5-tiers seeded, attachment to CardQuality, casual-deck-zero-S guard)
- [x] Card power-level scoring (0-100 per card). New `computeCardPower` in `cmd/hexdek-freya/advanced.go` runs upstream of `computeCardQualityTiers` and produces `dp.CardPowerLevels` — every non-land card gets a clamped sum of three explicit components: **ArchetypeFit (0-40)** weights card roles by the deck's primary-archetype fingerprint ratio (Combo deck favors RoleCombo/RoleTutor; Stax favors RoleStax/RoleRemoval), with a 10-point floor for any tagged card that didn't match the fingerprint; **CMCEfficiency (0-20)** uses an absolute curve band (CMC≤1: 20, CMC2: 18, CMC3: 14, CMC4: 10, CMC5: 6, CMC6+: 2) + 2-point bonus for multi-role at CMC≤2; **SynergyContribution (0-40)** rewards win-line piece (+25), value-chain bridge (+20) / step (+10), finisher (+12), synergy-cluster member (+6), per-role floor (+2 each), with penalties for redundant CMC4+ tutor when 3+ cheaper alternatives exist (-8) and CMC5+ Utility-only dead slot (-10). New `CardPowerLevel` struct exposes the headline Power plus all three component scores so downstream consumers (UI, hat strategy bridge) can see WHY a card rated where it did. Each existing star / solid / cuttable tier entry now echoes the same Power on its `CardQuality.Power` field — text report renders `★ [ 84] Thassa's Oracle — win condition piece`, `● [ 40] Demonic Tutor — flex slot`, `✂ [ 16] Ashaya, Soul of the Wild — high CMC with no clear role`. JSON: `card_power_levels` (full sorted list) + `power` on each star/solid/cuttable entry. 10 tests in `cmd/hexdek-freya/card_power_test.go` (range+sort, component-sum, archetype-fit Combo-vs-Stax differentiation, tagged-card floor, CMC-band exact values, multi-role bonus, synergy-component exact-point assertions per signal kind, dead-slot penalty clamp, tier-attachment, intuition spot-check)
- [x] Bracket rationale — explain WHY each bracket call was made. Refactored `estimateBracket` in `cmd/hexdek-freya/archetype.go` to return a `*BracketRationale` alongside bracket/label. New types `BracketSignal` (per-observation entry with Name, Kind, Tier, Measurement, Evidence card list, Contribution points, Note) and `BracketRationale` (FinalBracket, FinalLabel, RawScore, Signals slice). Every scoring signal (Game Changers, Tutor density, Combo lines, Average CMC, Fast mana, Counterspell density, Land ratio, Finisher density, Free interaction) records its threshold tier, measurement string, and (for curated lists like GC / free interaction) the actual card names that backed it. Every post-score adjustment (B5 gate, GC=0 ceiling, GC=1-3 ceiling, GC=1-3/4+/5+ floor, tuned-redundancy floor) records a `Note` explaining the demotion/lift trigger. Surfaced through `ArchetypeClassification.BracketRationale` → `DeckProfile.BracketRationale` → `jsonArchetype.bracket_rationale`; text report renders under "Bracket rationale (raw score N → BX label):" with per-line `[+N] Signal (tier): measurement — evidence` and `[gate/ceiling/floor] Signal: note`. Calibration test still 14/16. 4 new tests in `cmd/hexdek-freya/bracket_rationale_test.go`: per-tier contribution + measurement pinned across all 9 scoring signals, evidence cards attached for GC + free-int, B5 gate demotion recorded as Kind=gate, tuned-redundancy floor recorded as Kind=floor. Example output on azula B5 cEDH shows raw score 19, all 9 scoring signals with evidence (Vampiric Tutor, Force of Will, Mental Misstep, etc.), and confirmed B5 gate
- [x] B5 cEDH detection — free-interaction signal + confirmation gate. Survey of `estimateBracket` found B5 reached by raw score >=12 with no specific cEDH-shape requirement, so "optimized big-mana pile with 4 GCs + 10 fast mana + 8 finishers + 8% tutors" could falsely register as cEDH. Two additions in `cmd/hexdek-freya/archetype.go`: (1) curated `cedhFreeInteractionList` (~27 cards: Force of Will family, Fierce Guardianship / Deflecting Swat / Deadly Rollick commander-free, Mental Misstep / phyrexian-mana, Pact of Negation cycle, Subtlety / Endurance / Solitude / Grief / Fury evoke) — intentionally excludes cheap-but-not-free interaction (Counterspell, Swan Song, An Offer You Can't Refuse) since those are B4 staples, not the B5 discriminator. Counted into new `classifyContext.freeInteractionCount` field; +1/+2/+3 score contribution at 1/2/4 pieces. (2) B5 confirmation gate after the score-to-bracket switch: bracket=5 only if (`freeInteractionCount >= 2` OR `tutorDensity >= 0.12` OR `gameChangerCount >= 8`) AND `avgCMC < 2.8`. Otherwise demoted to B4. Decks that hit score 12+ via raw GC/fast-mana/finisher stacking but lack the deck-shape signature of cEDH get correctly demoted. Calibration re-run: 14/16 unchanged, none of 9 cEDH decks regressed (7/9 caught by free-interaction signal, the other 2 — Big Stick Voltron and Lumra Lands — pass via GC>=8 / tutor density markers). 5 new tests in `cmd/hexdek-freya/bracket_b5_test.go`: list-shape sanity + canonical anchors + negative cheap-interaction check, score-lift on free-interaction presence, B5 gate demotion path, low-CMC gate path, signal emission threshold
- [x] Card-tier "Solid Pick" middle bucket — `cmd/hexdek-freya/advanced.go` `computeCardQualityTiers` now emits a 3rd tier between Star and Cuttable: cards that scored above the cut floor but below the star threshold (window: `0.5 ≤ score < 3.0`, cap 5). Surfaces "okay-but-replaceable" cards so deckbuilders shopping upgrades know which slots are flex vs load-bearing. Default reason built from role mix + CMC when no per-card reason set (e.g. `"functional Tutor at CMC 3 — flex slot"`). `DeckProfile.SolidCards` field, `jsonDeckProfile.SolidCards` (key `solid_cards`), text-report renders under "Solid Picks:" with `●` glyph (mirrors the `★` / `✂` star/cuttable convention). Star scanning records starred names so solid scanning never duplicates them. 5 tests in `cmd/hexdek-freya/card_tier_test.go` (window placement, tier tagging + non-empty reason, star/solid exclusion, 5-card cap, default-reason role+CMC formatting). Verified on cedh_muldrotha: stars = Thassa's Oracle / Finale of Devastation / Deathrite Shaman; solid = Demonic Tutor / Eldritch Evolution / Survival of the Fittest / Spellseeker / Swan Song; cuttable = Ashaya, Soul of the Wild / Necrotic Ooze. Hat strategy bridge unchanged (solid tier is human-facing only)
- [x] Synergy clusters round 2 — chain-depth scoring. R60 round 1 (PR #129) added producer/payoff pair weighting; round 2 extends with a 3rd `clusterRoleAmplifier` role for themes whose engine is a natural 3-step chain. **death_value**: token-maker (producer) → sac outlet (amplifier) → dies/sacrifice trigger (payoff). **etb_value**: HasValueETB creature (producer) → blinker (amplifier) → triggers-on-other-ETBs (payoff). Scoring adds `min(producers, amplifiers, payoffs) * 3` chain bonus per cluster on top of the existing pair sum, so completing the chain (adding the missing-link card) creates a discontinuous score jump — matching the deckbuilding reality that a sac outlet finally makes a token+drain pile actually win. Membership extensions: `IsOutlet` cards now join the death_value cluster directly; token-makers get promoted to death_value when BOTH an outlet and a dies-trigger already exist in the deck (chain-context guard, avoids polluting every token deck). `rolesPairScore` updated so any two distinct non-unknown roles (P/A/Pf) form a chain pair at 2; Unknown takes precedence over Both for the dichotomy-free fallback. 5 new tests in `cmd/hexdek-freya/synergy_clusters_test.go`: amplifier-aware DeathValue + new ETBValue role classification, ChainBonus pinning concrete pair+chain math (P+P+A+Pf = 11 pairs +3 chain = 14; same with amp swapped out = 9, no chain), ChainScalesWithCompleteness (2/2/2 → +6 vs 3/1/2 → +3), NoChainBonusWithoutAmplifier (all-payoff cluster, chain=0). Verified against representative decks: cedh muldrotha gets death_value cluster (21), facedown_yarus picks up Ashnod's Altar / Evolutionary Leap as amps. Hat suite still green
- [x] Bracket calibration — B4 systematic underestimation fix. Baseline ran 11/16 exact on `data/decks/test/` (4 of 5 b4 decks undershot to b3 or b2). Root cause: GC-count ceiling rules (`GC=1-3 → cap b3`, `GC=0 → cap b2`) ignored the WotC combo carveout ("winning 2-card combo is itself a B4 marker, regardless of Game Changer count"). Fix in `cmd/hexdek-freya/archetype.go` `estimateBracket`: (1) lift GC=1-3 ceiling to b4 when `len(TrueInfinites) ≥ 1` OR `(finishers ≥ 8 AND fastMana ≥ 6)`; (2) lift GC=0 ceiling to b4 only when `len(TrueInfinites) ≥ 1` (strict WotC reading); (3) added GC≥5 → floor b4 (matches "many GCs = deliberately optimized"); (4) tuned-redundancy floor for finishers≥8+fast≥6 (e.g. voja tribal Voltron); (5) finisher count now contributes to base score (≥4: +1, ≥8: +2). Determined-loop counts intentionally NOT used for ceiling lift — over-classifies value engines (werewolf with 4 false-positive "determined loops" stays b3). New result: 14/16 exact, 16/16 within ±1 (was 15/16). Calibration regression test `cmd/hexdek-freya/bracket_calibration_test.go` pins ≥14 exact + per-deck ±1 invariant; skips when oracle data absent
- [x] Commander Spellbook import — `cmd/hexdek-freya/spellbook_import.go` parses the public variants JSON, infers loop type from features (`Win the game` / `Infinite *` → true_infinite), and dedupes against the 58 curated entries via canonical (lowercased + sorted) card-name keys; curated entries always win conflicts (richer outlets/stops). CLI flags `--spellbook <cache>` and `--spellbook-fetch <url>`; cache file gitignored. 11 regressions in `cmd/hexdek-freya/spellbook_import_test.go` (parse, status filter, type inference, key normalization, curated-wins dedupe, real-curated dedupe, intra-import dedupe, missing-cache no-op, round-trip, bad JSON, accessor merge)
- [x] Combo class hierarchy (PR #112) — `cmd/hexdek-freya/combo_class.go` taxonomy (true_infinite / soft_lock / value_engine / win_condition) with severity-weighted scoring; combos surface their class in `deckprofile.json` + strategy bridge so hat MCTS weight profiles can prefer true-infinite finishers over soft-lock value loops. Regressions in `combo_class_test.go`
- [x] Meta positioning expansion + reciprocity invariant (PR #139) — extended the 22-archetype matchup matrix with a reciprocity invariant (`matchup(A,B) + matchup(B,A) = 0` for symmetric matchups, ±1 tolerance for asymmetric advantage cases like prison-vs-aggro); flags any entry that violates as a matrix-build bug. Reports two-line reasoning per matchup. Regressions in `meta_positioning_reciprocity_test.go`

### Done (2026-05-23)
- [x] 4-card combo detection — `checkQuadCombo` enumerates all 24 permutations via nested distinct-index loops (structurally generated, not hand-listed); prefiltered to flow-active candidates and capped at 70 to bound runtime at ~3s worst case. Regressions in `cmd/hexdek-freya/quad_cycle_test.go`; runtime tradeoff in `docs/freya-4card-runtime.md`

### Done (2026-05-22)
- [x] Fix triple combo cycle ordering — enumerate all 6 permutations in `checkTripleCombo` (was 2/6); regressions in `cmd/hexdek-freya/triple_cycle_test.go`

### Done (2026-04-29)
- [x] Fix combo false positives (self-exile, hand vs battlefield, attack-trigger dependency, randomness)
- [x] Wire 20 `archetypes.go` definitions into classifier (11 new fingerprints + context signals + gameplan descriptions)
- [x] Add value chain templates for Storm, Artifact, Enchantress, Counters Matter engines
- [x] Improve Frank Karsten land formula (ramp/draw discount with dork fragility weight, 28-land floor)
- [x] Fix bracket estimation to exclude banned cards from scoring
- [x] Track colorless `{C}` mana production in land analysis
- [x] Refine card role multi-tag priority ordering (strategic importance sort)
- [x] Eval weight profiles for all 22 archetypes (up from 5 generic profiles)
- [x] Mana curve shape analysis (bimodal detection, top-heavy/bottom-light warnings)
- [x] Expand KnownCombos database (58 entries, +16: Worldgorger, Nim Deathmantle, Breach, Tooth&Nail, Karmic/Reveillark, persist combos)
- [x] Commander synergy scoring (theme extraction from oracle text, 14 theme patterns, synergy % in deck profile)
- [x] Interaction quality scoring (avg CMC of interaction, cheap vs expensive breakdown)
- [x] Recursion depth scoring for value chains (infinite/deep/shallow/none loop-back detection)
- [x] Protection density analysis (built-in protection tracking for combo/threat pieces)
- [x] Mana base grading (tapland/fetch/utility scoring, A-F grade, upgrade suggestions)
- [x] Threat assessment profile (26-entry hoser database, condition-matched vulnerability report)
- [x] Opening hand simulation (10K Monte Carlo trials, keepable % and avg turn to 4 mana)
- [x] Synergy clusters (theme-grouped card packages with pairwise synergy scoring)
- [x] Meta positioning (archetype-vs-archetype matchup predictions with reasoning)
- [x] Card quality tiers (star/cuttable identification from role overlap, win line presence, CMC efficiency)
- [x] Color weight optimization (demand vs supply analysis, specific land swap suggestions)
- [x] Deck personality blurb (archetype-aware 2-3 sentence flavor description)
- [x] Power percentile within archetype (multi-factor scoring: tutors, mana base, interaction, draw, curve, hands)

### Backlog
- [ ] 5-card+ combo detection (would need graph-walk, not brute-force per `docs/freya-4card-runtime.md`)
- [ ] Tutor inference for modal spells and complex wording
- [ ] NLP-grade oracle text parsing (replace substring matching for edge cases)

## Issue Log

> **Rule:** ANY time a test, audit, Goldilocks run, Loki fuzz, or manual investigation surfaces a bug, invariant violation, or unexpected behavior — log it here IMMEDIATELY. No exceptions. Format: date, source, description, status.

### Open

| Date | Source | Issue | Severity | Notes |
|------|--------|-------|----------|-------|
| 2026-05-30 | Loki r60 Phase G seed-43 cross-validation | **ZoneConservation x38 — Silk, Web Weaver // Silk, Web Weaver + Opaline Bracers fabrication on seed 43 game 4557** (turns 38-60). Same fabrication SHAPE as the closed Aziza signature (`present in a zone but not in (Minted - Ceased)`), distinct CAUSE: both cards `h0OGVC*` OG-provenance on the same seat, persisting unspliced across many turns. NOT a §707.10 spell-copy class — MintSpellCopy fix doesn't address it. Signature suggests a commander-DFC + equipment-cycling lineage gap (Silk is a DFC with both faces named the same; Opaline Bracers is equipment that may be cycling its AttachedTo target). **2026-06-12 judge sweep round 2 datapoint: same class hit again — Pathrazer of Ulamog `h3OGVC1100042`, seed 7777 game 2430 (turns 36-42, 14 ticks), plain single-faced Eldrazi creature with NO DFC and NO equipment in the lineage — which WEAKENS the commander-DFC + equipment-cycling hypothesis; the common factor across Silk/Bracers/Pathrazer is just `h*OGVC*` OG-provenance persisting unspliced. Cheaper repro now available.** | medium | Surfaced by Phase G cross-seed verify (`docs/zonecons-phase-g-aziza-r60.md` cross-seed section). Queued as Phase H scope. Reproduce with `./hexdek-loki --games 5000 --seed 43 --invariant zone-conservation --nightmare-boards 0 --instanceid-strict-census --violations-dump /tmp/dump.tsv`; game 4557. **Cheaper repro (r62): `--games 2500 --seed 7777 --nightmare-boards 0 --instanceid-strict-census`; game 2430 (Pathrazer of Ulamog, seat 1).** |
| 2026-06-12 | hexdek-judge sweep (`--check-mana-costs`) | **2 oracle corpus entries fail the CR §202.2 mana-symbol grammar — Boulder Jockey `{2}{R}{D}` and Keeper of the Crown `{2}{L}` (both set `mb2`)**. `{D}` and `{L}` are Mystery Booster 2 playtest "un-set" symbols that genuinely aren't in the CR grammar — a corpus-data oddity, not an engine bug. Decide whether the judge's checker should carve out acorn/playtest sets (so `valid:false` on the report stops crying wolf) or whether the importer should exclude mb2 playtest cards from the corpus entirely. | info | Full report at the sweep artifact (`hexdek-judge --check-mana-costs`); 30,477 scanned, these are the only 2 hits. |
| 2026-06-12 | hexdek-judge sweep round 2 | **4 direct `.Life -=` sites bypass the new §104.1-2 post-game-end guard** added to `LoseLife`/`DealDamage` (the two chokepoints that fire `life_lost`): `combat.go:1708` (combat damage to player), `resolve.go:843` + `resolve.go:1234` (AST damage effects), `ward_alt_payment.go:390` (ward life payment). Same family as the fixed winner-drain class — an in-flight resolution running past game-end through one of these sites can still mutate life on the ended snapshot. Not observed in 3,500 sweep games (the observed class flowed through `LoseLife`); sweep-clean does not prove unreachable. Recommended: route all four through `LoseLife`/`DealDamage` or share one `lifeMutationAllowed(gs)` guard. | low | Sibling cleanup to the r62 `CheckEnd` pre-sweep fix (see Resolved row below). |
| 2026-06-12 | legality-validator r62 (ride-along, `hexdek-loki -legality`, PR #1021) | **Five findings from the first 300-game chaos run (316 violations; 0 from the invariant census in the same games — a disjoint bug surface). Four of five closed within a day of the validator landing:** (1) **117.1a mid-stack cast cluster ×302 — FIXED** (#1025, commander-cast drain; see Resolved). (2) **601.2f-h +1 over-pay ×9 — 8/9 FIXED** (#1024: extort/auxiliary in-window mana payments weren't credited to the cost observation); 1 residual shape OPEN with a different mechanism (Martyr's Cause over-pays +1 with zero in-window adds/aux-spends — repro `hexdek-loki -games 106 -seed 42 -legality`, game 105 turn 33 seat 2; verified present on pristine main, unrelated to the chokepoint routing). (3) **608.2c announced-illegal activation targets ×5 — FIXED** (#1023: sacrifice-cost activations no longer announce targets doomed by their own cost). (4) **Two mana-add sites bypassing the `AddMana` chokepoint — FIXED** (AddMana-chokepoint PR: Birgi's cast-trigger observer in cast_counts.go and `resolveAddMana` in resolve.go now route through `AddMana` proper — canonical add_mana event, future mana-replacement visibility, single validator credit; per-site NoteManaAdd stopgaps removed; 300-game verify clean, no over/under-pay reintroduced). (5) **`PriorityRound` response-cast path bypassed `CastSpell` entirely — FIXED** (#1026: bounded convergence at the response site — CalculateTotalCost with cost statics, §117.1a/§307.1 timing rejection of sorcery-speed responses, CounterCanTarget + ValidateTargetsAtAnnouncement on announced targets, validator hooks wired). **All five findings closed.** | med | Evidence: `/tmp/fable-review/legality-validator-r62-report.md` + PR #1021. Residual: the Martyr's Cause over-pay shape under item (2). |

### Resolved

| Date | Source | Issue | Resolution |
|------|--------|-------|------------|
| 2026-06-12 | hexdek-judge sweep round 2 (seed 99 layers-seeded, game 225) | **CheckEnd crowned a §104.2a "winner" at life=0 — simultaneous loss misresolved as a win (SBACompleteness "704.5a missed" ×2)**. Diagnostic replay traced the exact sequence: end-step Zurgo Stormrender cascade → §704.5a marks the last opponent Lost mid-SBA-pass → the SAME in-flight trigger chain drains the surviving seat from 1 to 0 (event `life_change from:1 to:0`) → next `CheckEnd` caller ran NO fresh SBA pass and declared the 0-life seat the §104.2a winner. Per CR §704.3 + §104.4a the pending §704.5a loss must apply first → both lose → draw. | Two-part r62 fix. (1) **`CheckEnd` pre-sweep** (`multiplayer.go`): run the leaf player-loss SBAs (704.5a/5b/5c) before evaluating game-over, gated on `ended != 1` so repeated post-end calls can't retroactively change a recorded outcome (full `StateBasedActions` would recurse back into CheckEnd). Game 225 now resolves as a §104.3b `simultaneous_elimination_draw`. (2) **Post-end life-mutation gates** on `LoseLife` + `DealDamage` (`state.go`): trigger dispatchers already refuse to START handlers on `ended=1`, but a handler mid-resolution when its own effects end the game keeps executing — its trailing drains were mutating the winner on the ended snapshot (observed: the same game-225 chain kept sacrificing/draining post-end). 3 regression tests in `internal/gameengine/post_end_life_mutation_test.go` (chokepoint no-op post-end; mid-flight chain can't drain the winner; simultaneous-zero-life resolves as draw with seat NOT marked Won). Verified: seeded 1000-game re-run → game 225 cleared, **1000/1000 clean**. Sweep totals this round: 2,500 games seed 7777 strict (14 ZoneConservation hits, 1 game — Pathrazer datapoint appended to the open Phase-H row) + 1,000 games seed 99 layers/replacement/combat-seeded (the 2 SBACompleteness hits fixed here) + 1,000-game verify. |
| 2026-06-12 | legality-validator r62 finding (1) | **117.1a mid-stack cast cluster ×302 (302 of 316 first-run violations)** — sorcery-speed casts by the active player with an item already on the stack, clustered on commander-cast turns. | Root cause: `CastCommanderFromCommandZone` PUSHES the commander spell and returns (documented caller-drives-resolution contract, commander.go), but `tryCastCommander` — its only production caller — only ran SBAs afterward. The commander spell squatted unresolved on the stack: every later main-phase cast that turn announced mid-stack (§117.1a/§307.1), later spells LIFO-resolved BEFORE the commander, and the commander resolved as a bystander of the next cast's drain. Attribution proven by capturing the resident item's name at announcement: 12/12 pre-fix residents were commander spells (Chainer, Laquatus, Yawgmoth, Kenrith, …). **Fix:** mirror `CastSpell`'s tail in `tryCastCommander` (PriorityRound + DrainStack after a successful cast). 117.1a count on the 85-game seed-42 repro: **98 → 0**; the other validator findings (8× 601.2f-h, 3× 608.2c) unchanged, as expected. Regressions: `internal/tournament/commander_cast_drain_r62_test.go` (stack empty + commander on battlefield after tryCastCommander; zero mid-stack violations on a follow-up cast with the validator attached — fails pre-fix naming the resident commander). Diagnostics added to the validator: `ResolveFrameDepthAtAnnounce` + `StackTopAtAnnounce` on the observation (these are what cracked the case). |
| 2026-06-12 | hexdek-judge CR-compliance sweep | **Reactive sweep: 3,800 chaos games across seeds + census modes, zero violations; stale 2026-05-30 event-registry Open row adjudicated** — sweep runs: 300 + 1,500 games seed 42 standard, 1,500 games seed 42 `--instanceid-strict-census`, 500 games seed 1337 strict (all `--nightmare-boards 0`, vs origin/main `4e36e46a` post-r62): **0 crashes / 0 violations / 3,800 clean games**; empty violations dumps. The r59–r62 fix wave holds at this depth — chaos-game surface is clean. `--check-mana-costs` corpus sweep found only the 2 mb2 playtest-symbol oddities (new Open row above). | Closed as a clean-sweep record. The interesting yield was adjudicating the stale Open row below. Reproduce: `./hexdek-loki --games 1500 --seed 42 --nightmare-boards 0 [--instanceid-strict-census] --violations-dump /tmp/v.tsv`. |
| 2026-05-30 | `docs/engine-event-registry.md` §3.1 audit | **9 per_card handlers registered on phase-step events the engine never emits — 7 `postcombat_main_controller` cards (Tymna the Weaver, Kona Rescue Beastie, Megatron Tyrant + DFC face, Sorin of House Markov + DFC face, Kirri Talented Sprout) + 2 `upkeep_opponent` cards (Slicer Hired Muscle + DFC face)**. Claimed: "there is no postcombat-main-phase fire surface at all" and the 9 cards silently never trigger; recommended ~50 LOC engine PRs to add both emissions. | **Adjudicated stale-incorrect by the 2026-06-12 hexdek-judge sweep — both events DO fire and predate the audit.** The audit only inspected `gameengine.FirePhaseTriggers` (phases.go), but the tournament turn driver emits both: `upkeep_opponent` at `internal/tournament/turn.go:203` and `postcombat_main_controller` at `turn.go:355` (main-2 boundary), present since commit `214f05ca` (2026-05-01) — four weeks BEFORE the audit. Nothing pinned the end-to-end path, so the claim was undecidable from tests; now pinned by `internal/tournament/postcombat_main_trigger_test.go` (TakeTurn with Tymna + a stamped opponent-hit flag → handler pays 1 life, draws 1, emits `tymna_the_weaver_postcombat_draw` with `paid:true`). PASSES on main. Caveat kept honest: the EVENT surface is closed for all 9 cards, but Slicer's `upkeep_opponent` handler body is itself an `emitPartial` stub (`per_card/slicer_hired_muscle.go:45-51` — "opt_in_control_change_or_convert_branch_not_modeled"), so Slicer's gameplay gap persists handler-side; that is per-card handler debt, not an engine event gap. Lesson for future audits: emission surfaces live in BOTH `gameengine/phases.go` AND `internal/tournament/turn.go` — grep both before declaring an event never-fired. |
| 2026-05-31 | `dev/pathological-stress-wave2-r60` (this PR) | **Wave-2 pathological-deck stress gauntlet expansion** — Verification Phase 5 follow-up to wave 1 (`docs/loki-pathological-r60.md`, PR #950 — 14 cards × 4 seats round-robin, surfaced 1 game / 2 hits on `CombatLegality` against "Wall of Tanglecord" attacking with defender). Wave 1 covered 10 interaction surfaces (copy chains, blink loops, no-op loops, cascade, reanimator, token-of-token, infinite-mana, tutor, counter-trigger token mint, paradigm copy); the gauntlet's design intent was additive surface coverage, and 10 historically-fragile interaction families were NOT in the wave-1 list: storm count, pillowfort attack-tax, mass land destruction, counterspell-tribal, stax forced-sac, alt-Eldrazi annihilator (wave-1's Eldrazi was "Aeons Torn" only), theft/control-change-revert, sacrifice tutor chain, and Sneak Attack / Show and Tell free-cheat permanents. | Closed. Ran wave 2 with **17 cards × 4 seats** spanning the 10 new themes (Tendrils of Agony + Storm Crow / Ghostly Prison + Propaganda / Armageddon + Wildfire / Talrand, Sky Summoner + Counterspell / Smokestack + Tangle Wire / Emrakul, the Promised End + Ulamog, the Ceaseless Hunger / Insurrection + Threaten / Razaketh, the Foulblooded / Sneak Attack + Show and Tell) at `--games 2000 --seed 42 --nightmare-boards 0 --instanceid-strict-census` — **0 crashes / 0 violations / 2000 clean games / 4m 6s / 8 g/s** (vs wave 1's 0/2/1999, same depth). Empty `v.tsv` (0 bytes) confirms no violations dumped. Full doc at `docs/loki-pathological-wave2-r60.md` with per-theme rationale, run command, results table comparing wave 1 / wave 2 / PR #896 standard 5k baseline, interpretation (the 10 newly-themed surfaces are engine-clean at depth + the chaos-deck generator was selective at seed 42 + a wave-3 candidate list — extra-turn chains, graveyard recursion toolboxes, proliferate cascades, life-swap loops — for future runs), and reproduction commands. **Card-list separator note documented**: the Loki flag parser at `cmd/hexdek-loki/main.go:374-382` auto-detects `;` as the separator when present in the input string; wave 2's list contains comma-bearing card names ("Talrand, Sky Summoner" / "Emrakul, the Promised End" / "Ulamog, the Ceaseless Hunger" / "Razaketh, the Foulblooded") that would fragment with the default `,` separator, so wave-2 invocations MUST use `;`. Wave 1's 14 cards happened to have no commas so the default worked there. Combined wave-1 + wave-2 coverage: 24 distinct pathological cards, 4000 games at seed 42, exactly the 1 wave-1 `CombatLegality` signature, no new bugs. |
| 2026-05-31 | `dev/parity-engine-setup-r60` (this PR) | **No cross-engine parity setup against xmage (Verification Phase 4 prep) + sibling-merge collision: PR #933 `ReverseIndex` function name collided with the older `ReverseIndex` interface in `recommender.go:44`** — the `internal/paritycheck` infrastructure is fully built out for **Go ↔ Python** parity (canonical `Event` / `Outcome` / `ReplayData` / `Divergence` / `ParityReport` types, `RecordGoGame`, `RunPython`, `Diff`, `Run` orchestrator, markdown writer, 4 unit tests) but four discrete gaps block any actual cross-engine run today: (a) the Python harness `scripts/parity_harness.py` referenced by `RunPython` doesn't exist in the repo — system runs in `python_available: false` mode (no false-positive parity claims, but Go ↔ Python promise has never actually been driven); (b) no xmage adapter at all; (c) no game-state JSON ingest — engine has no `MarshalGameState`/`UnmarshalGameState` round-trip, only deck-seed playthrough via `RecordGoGame(nSeats, decks, seed, gameIdx)`; (d) no Makefile or CI entry script. Separately, PR #933 (HUGINN 2.0 Worker A's reverse-index loader, merged 2026-05-31) added `func ReverseIndex(oracleID string) []string` at `internal/huginn/reverse_index.go:329` but the older `type ReverseIndex interface { … }` had been declared in `internal/huginn/recommender.go:44` — duplicate-declaration build failure on main that blocked all downstream packages (including `internal/paritycheck` via the `tournament → huginn` import chain). | Closed. **Build unblocker**: renamed the function to `LookupInstancesByOracle` (descriptive, non-colliding); preserved the interface type for the contract callers depend on. Only test-file callers existed (`reverse_index_test.go`) — updated 13 call sites via Python regex. Build clean on `go build ./...`. **Parity setup**: shipped `docs/cross-engine-parity-setup.md` documenting current state, the four gaps, an xmage adapter sketch (mage.game.events.GameEvent dispatcher, MatchOptions seed surface, RNG non-identity caveat), and a Phase-4 handoff section. **17 scenario JSON skeletons** in `data/parity-scenarios/` covering combat-fundamentals (3) / mass-effects (2) / etb-cascade (2) / combo-wincons (3) / replacement-effects (2) / stack-interactions (2) / multiplayer-dynamics (2) / edge-cases (1), all `mode: "deck_seed"` against the existing `data/decks/test/` calibration corpus with distinct seeds (101-801). `mode: "state_inject"` reserved in the schema for the post-engine-round-trip future. **`scripts/parity-test.sh`** as CI entry point — exit 0 (parity) / 1 (divergence or `--skipped-as-fail`) / 77 (autotools-convention "test was unable to run, don't count as failure" when xmage adapter missing). Supports `--tag TAG` filter (verified `--tag combat-fundamentals` resolves to 3 scenarios) and `HEXDEK_XMAGE_JAR` env-var override. **7 scenario-schema regression tests** in `internal/paritycheck/scenario_schema_test.go`: non-empty (floor at 15 with 17 shipped), unique-names, filename↔name invariant, required-fields-present, deck_seed-specific fields (n_seats ∈ [2,4], deck_pool length >= n_seats, every deck path resolves), state-inject-mode-not-shipped guard (rejects premature state_inject scenarios until the engine round-trip lands), tag distribution log + cross-cutting `phase4-bootstrap` invariant (all 17 carry it). Phase-4 verification work explicitly handed off in doc §6: write Python harness as the structural template → write xmage Java adapter (~200 LOC subscribing to `mage.game.events.GameEvent`) → add `--xmage-harness` flag to `cmd/hexdek-parity` → land `GameState` JSON round-trip in separate engine PR → wire the CI script to a real job. Full `internal/paritycheck/` + `internal/huginn/` suites pass post-build-unblocker. |
| 2026-05-31 | `dev/muninn2-instanceid-aware-r60` (this PR) | **Genesis Chamber detection (PR #815's 22,662-hit silent no-op surface) was a manual scoping step during a coverage-sweep PR — no CLI surface automates it across recent observation data** — `internal/muninn/muninn.go:200 PersistDeadTriggers` already implements the silent-inert detection pattern (handler `TriggeredCount > 0` AND `DamageDealt == 0` AND `KillsAttributed == 0` AND `!ContributedToWin` AND not the WinningCard AND not a land/token) and persists hits to `dead_triggers.json` from every tournament run with `--audit --analytics`; the data is right there. But the only consumer was `hexdek-muninn --triggers`, which prints the raw count-ordered list without severity tiering, machine-readable output, or a CI-fail gate. **Stale callout in the dispatch framing**: the task envisioned cross-referencing with an "InteractionGraph" from "dev-4 / dev-14" workers that doesn't exist (`grep "InteractionGraph"` returns zero hits across the repo + no in-flight branches build it). InteractionGraph integration will fold in as a fifth gate to `PersistDeadTriggers` itself when that graph lands (separate PR); the existing four-gate signature is the production-grade detector that already caught Genesis Chamber. | Closed. Added `cmd/hexdek-muninn/--silent-inert-audit` subcommand on top of the existing `ReadDeadTriggers` surface. New flags: `--silent-inert-audit` (run), `--audit-format text/json/tsv`, `--audit-threshold-critical N` (default 1000), `--audit-threshold-moderate N` (default 100), `--audit-threshold-watch N` (default 10), `--audit-fail-on-critical` (CI exit-code gate). Severity tiers: **critical** ≥1000 hits (Genesis Chamber at 22,662 sits deep in this tier) · **moderate** 100-999 · **watch** 10-99 · **trivial** <10 (excluded from text output, kept in JSON/TSV for completeness). Output formats: text (human, banner + per-tier sections), JSON (stable wire format with thresholds-echo for dashboards), TSV (spreadsheet/grep pipelines). 8 regressions in `cmd/hexdek-muninn/silent_inert_audit_test.go`: tier-boundary semantics (>= comparison, Genesis Chamber → critical, exactly-at-threshold → higher tier), buildCandidates Genesis Chamber scenario (4-entry fixture with each tier represented; pins per-game-average arithmetic 22662/142 ≈ 159.6), summarize tier counts, text format (banner + critical line + trivial excluded), `TestRunSilentInertAudit_FailOnCritical` (exit 1 with critical present + flag set; exit 0 when threshold raised above 22,662), JSON pipeline shape decode round-trip, TSV header + critical-row cells, unknown format rejection path. CLI smoke-tested against an empty Muninn dir (emits "no records found, run a tournament with --audit --analytics"). Future regression: any handler that ships with a ctx-key bug or other silent-inert defect now surfaces in a CI-actionable form within one tournament run, rather than waiting for a manual scoping PR. Full per_card + engine root + muninn suites pass. |
| 2026-05-30 | `dev/engine-event-integration-tests-r60` (this PR) | **No cross-handler regression test surface — per_card handler base (~1200 OnTrigger registrations) only exercised via per-card unit tests** — follow-up to PR #887 (engine-event registry doc). PR #887 enumerated 79 distinct `FireCardTrigger` event names with 91 distinct per_card listener names, but the only regression pressure on the handler base came from the per-card unit tests (one handler per file). Cross-handler regressions — a sibling refactor introducing a panic into a handler whose own test doesn't drive the right path — went uncaught until production / Loki fuzz / Goldilocks discovered them, often weeks later. The 2026-05-30 per_card ctx-key audit waves 1-5 (PRs #815 / #824 / #841 / #867 / r60) closed 24 such handler bugs that had been silently inert in production for months; same audit estimated the remaining ~1000 cards "spot-audited for the same pattern — no further silent-inert ctx-key bugs surfaced" but without integration-level pressure, "spot-audited" was the best available guarantee. | Closed. Added `EnumerateOnTriggerRegistrations()` to the per_card Registry as a small introspection API (returns `[]struct{CardName, Event string; HandlerCount int}` snapshot). Wrote `internal/gameengine/per_card/engine_event_integration_test.go` with three pinned tests: **`TestEngineEventIntegration_NoHandlerPanics_BaselinePin`** is the load-bearing pin — walks EVERY `(cardName, eventName)` pair in the global registry, places a stub permanent of `cardName` on seat 0's battlefield, fires the canonical event via `FireCardTrigger` with a kitchen-sink ctx (every key any handler in the per_card base might read: `perm` / `permanent` / `attacker_perm` / `defender_perm` / `source_perm` / `damager_perm` / `target_perm` / `src` / `entering` / `leaving` / `card` / `card_name` + every seat-shape key + numeric / phase / string / boolean defaults), recovers any panic, and asserts zero panics across the full base. Baseline (pinned 2026-05-30): **1216 pairs walked, 1268 handlers exercised, 0 panics, 0.06s wall-clock**. Floor pinned at `len(regs) >= 1100` to allow natural shrinkage from handler consolidation without forcing test updates; ratchet up as the per_card base grows. **`TestEngineEventIntegration_RegistryEnumerationIsStable`** pins the introspection API — successive `EnumerateOnTriggerRegistrations()` calls return matching length + handler-count sums (guards against a future Registry refactor breaking the API the baseline test depends on). **`TestEngineEventIntegration_RegistryNonEmpty`** pins distinct canonical event count `>= 50` and the high-traffic events (`permanent_etb` / `attack` / `spell_cast` / `deals_combat_damage` / `die` / `permanent_ltb`) each have at least one handler — catches a registry-reset regression that wipes handlers OR an over-aggressive alias consolidation that collapses a load-bearing canonical. Caveat noted in the test docstring: the integration value is panic-detection across the full handler base, not behavioral correctness — handlers that gate on specific ctx shapes will skip cleanly; ones that read what's there get a sensible default rather than nil-deref. Behavioral pinning lives in the per-card unit tests as before. Future regression PRs introducing a panic into a previously-quiet handler trip this test immediately, regardless of whether the panicking card has its own dedicated test. Full per_card + engine root suites pass. |
| 2026-05-30 | per_card ctx-key audit waves 1-5 (PRs #815 / #824 / #841 / #867 / this PR) | **per_card silent-inert ctx-key bug class — structurally closed across 5 waves, 24 handler bugs total** — recurring pattern where per_card OnTrigger handlers read a context key the engine never writes for the registered event. Pre-fix, handlers returned silently at the type-assertion-then-nil-check gate, OR partial-fired with default-zero values that worked accidentally for one specific seat. Surface spanned 8 event families: permanent_etb / nonland_permanent_etb (9 handlers, wave 1) · combat_damage_player (4 handlers, wave 2) · creature_attacks (5 handlers, wave 3 + 1 missed → wave 4 + 1 more → wave 5) · card_exiled (1 handler, wave 4) · permanent_phased_out (1 handler, wave 4) · scry / roll_dice / artifact_tapped_for_mana / card_discarded (4 handlers, wave 5). Common root causes: (a) handler reads `ctx["permanent"]` while engine writes `ctx["perm"]`; (b) reads `ctx["seat"]` while engine writes `attacker_seat`/`controller_seat`/`discarder_seat` depending on event; (c) reads `ctx["damager_perm"]`/`ctx["source_perm"]` on combat_damage_player (engine writes name string + seat int, no perm); (d) reads `ctx["defender_seat"]` on creature_attacks (engine doesn't populate — defender lives on the attacker via `setAttackerDefender`, looked up via `AttackerDefender(perm)`); (e) reads `ctx["count"]` / `ctx["mana_type"]` keys that don't exist for the registered event; (f) shared helper reused across two events with different ctx shapes (Syr Vondam creature_dies vs card_exiled). | **Class closed.** Wave 5 (this PR) wraps the audit by clearing the remaining keyword-event surface (scry / roll_dice / artifact_tapped_for_mana / card_discarded + a second pass on creature_attacks). The hypothetical events the user named to scope wave 5 (`mana_paid` / `counter_added` / `card_revealed` / `library_searched` / `spell_resolved` / `ability_activated`) don't exist as engine fire sites — empty surface, nothing to audit. **Closure pattern** for future per_card handler authors: (1) ALWAYS check `internal/gameengine/*.go` for the actual `FireCardTrigger(gs, "event_name", ctx)` call before writing a handler — the ctx keys are NOT a stable contract, each event has its own shape. (2) For attack-event defender lookup, use `gameengine.AttackerDefender(attacker_perm)` — never read `ctx["defender_seat"]`. (3) For combat_damage_player attacker lookup, resolve by name on `gs.Seats[source_seat].Battlefield` — never expect a `source_perm`/`damager_perm` key. (4) Adopt the canonical-key-with-legacy-fallback pattern (`v, ok := ctx["canonical"]; if !ok { v, _ = ctx["legacy"] }`) when refactoring an existing handler, so Hat/test callers that explicitly thread the legacy shape don't break. (5) Always read shared helpers carefully when they bridge events with different ctx shapes — Syr Vondam's `creature_dies`/`card_exiled` shared helper was the worst offender. **Cumulative audit results:** wave 1 fixed 5 + audited 9 (PR #815); wave 2 fixed 4 + audited combat_damage surface (PR #824); wave 3 fixed 5 + audited attacks surface (PR #841); wave 4 fixed 3 + audited 7 more event families (PR #867); wave 5 (this PR) fixed 7 across 6 handlers + audited the remaining keyword surface. **34 new regressions across 5 test files** pin the canonical engine ctx shape for each fix. The remaining per_card handler base (~1000 cards) has been spot-audited for the same pattern — no further silent-inert ctx-key bugs surfaced. If a future handler reads a ctx key, cross-check against the actual fire-site BEFORE writing the gate. |
| 2026-05-30 | `dev/engine-event-registry-doc-r60` (this PR) | **Engine event registry doc + dead-handler audit + alias closure for 4 cards** — no prior doc enumerated the engine's `FireCardTrigger` event surface (79 distinct event names, 169 alias entries, 91 per_card listened names). Audit on this branch cross-referenced fired-vs-listened via the canonical `event_aliases.go` `NormalizeEventSingle` route and surfaced **4 canonical events with dead handlers** (13 handler registrations across 11 cards): `untap_step` (Seedborn Muse, Rasputin Dreamweaver — engine fires `untap`), `draw_step_controller` (Sylvan Library, Nekusar — engine fires `draw_step`), `postcombat_main_controller` (Tymna, Kona, Megatron, Sorin of House Markov, Kirri — engine has no postcombat-main fire), `upkeep_opponent` (Slicer — engine `upkeep` is active-seat-only). Pre-audit none of these 11 cards' triggers fired in production — Tymna in particular is a top-tier cEDH partner commander silently drawing zero cards from combat damage. Plus 17 "dead events" (canonical events fired with zero per_card `OnTrigger` listeners) — most are intentional audit-event surfaces (`bargain_paid`, `battalion_triggered`, `gift_paid`, `suspected`, etc.) where the rules-payload is consumed via different hooks (OnResolve / OnETB / OnCast reading `CostMeta` flags or counter state). | Closed via two ~1-line additions to `internal/gameengine/event_aliases.go`: `"untap_step": {"untap"}` and `"draw_step_controller": {"draw_step"}`. Both registration and dispatch run through `NormalizeEventSingle` (`per_card/registry.go:307` + `:516`), so the alias routes existing handlers without any per_card-side edit. 4 cards' triggers (Seedborn Muse, Rasputin Dreamweaver, Sylvan Library, Nekusar) move from silent-no-fire to live. 4 regressions: `TestEventAlias_DeadHandlerClosure_PhaseStepAliases` pins the alias table itself; `TestSeedbornMuse_FiresOnEngineUntapEvent` is the load-bearing end-to-end pin (3 tapped perms on seat 0 → engine fires `untap` with `active_seat=1` → all 3 untap); `TestSeedbornMuse_DoesNotFireOnOwnUntapStep` defends against over-firing (handler's `active_seat == controller` gate must still skip); `TestSylvanLibrary_FiresOnEngineDrawStepEvent` confirms the handler runs end-to-end via `per_card_handler` audit emit. The 9 remaining cards (7 `postcombat_main_controller` + 2 `upkeep_opponent`) need engine-side `FirePhaseTriggers` extensions (not alias additions) — logged under the Open issues table as 2026-05-30 `engine-event-registry §3.1 audit` for a follow-up engine PR. New doc at `docs/engine-event-registry.md` enumerates the full surface, the audit script, the 17-row intentional-audit-event table, and the open-engine-gap surface. Full per_card + engine root suites pass. |
| 2026-05-30 | `dev/event-kind-normalize-wave3-r60` (this PR) | **Event-Kind normalize wave 3 — bypassed `DiscardCard` helper + silent §702.34a Madness / §702.187 Mayhem / Necropotence / `card_discarded` trigger / `Turn.Discarded` stat rules-bugs** — follow-up to wave 1 (PR #830 lose_game) + wave 2 (PR #849 untap). The canonical `DiscardCard` helper at `internal/gameengine/resolve.go:994` is the documented discard chokepoint, with the explicit docstring invariant: *"All discard paths should route through this to ensure Liliana's Caress, Waste Not, Tergrid, etc. see every discard."* That helper owns: CR §702.34a Madness replacement (`OnDiscardMadness` exiles + registers cast permission), CR §702.187 Mayhem turn-tracking (`MayhemDiscards` map), Necropotence skip-draw rerouting (graveyard → exile when active), the `card_discarded` trigger (Liliana's Caress / Waste Not / Tergrid / Anje Falkenrath / Bone Miser / Captain Howler — every per_card discard-trigger handler), `Turn.Discarded` stat, and `gs.Flags["discarded_N"]` for evalCondition. Audit found 6 high-impact sites bypassing the helper with direct `seat.Hand[i:]` splices + raw `seat.Graveyard = append(...)` calls: (a) `internal/gameengine/activation.go:400-418` — **THE engine activation-cost discard path** for every "{T}, Discard a card: …" activated ability; (b) `internal/gameengine/ward_alt_payment.go:308-319` — Saruman-style ward alt-payment discard; (c) `internal/gameengine/per_card/mox_diamond.go:63-74` — Mox Diamond's "discard a land" ETB cost; (d) `internal/gameengine/per_card/emet_selch.go:86-98` — Emet-Selch's highest-CMC loot discard; (e) `internal/gameengine/per_card/smellerbee_rebel_fighter.go:60-75` — Smellerbee's discard-entire-hand attack trigger; (f) `internal/gameengine/per_card/per_card_batch_k_r60.go:184-195` — generic discard-on-draw batch helper. Load-bearing rules-bug fallout pre-fix: a madness card discarded via Mox Diamond / Emet-Selch / Smellerbee / any activated discard-cost ability silently went to graveyard instead of being Madness-exiled, breaking every Madness deck's interaction with these effects; Liliana's Caress / Waste Not / Tergrid never registered any of those discards (no `card_discarded` trigger fired); `Turn.Discarded` undercounted across all 6 paths breaking the discard-matters archetype detection in Freya. | Closed. All 6 sites refactored to call `gameengine.DiscardCard(gs, card, seat)` directly. The downstream `Kind="discard"` `LogEvent` emit at each site is **preserved**, because `internal/hexapi/showmatch.go:4065` reads it for the live spectator-log chat-line formatter (`"COMMANDER DISCARDS LIGHTNING BOLT"`) — `card_discarded` is a per-card trigger Kind, not a LogEvent Kind, so the two are not interchangeable for the spectator surface. 7 regressions across two test files: **`internal/gameengine/discard_canonical_routing_r60_test.go`** pins the engine-side activation-cost path (3 tests: `Turn.Discarded` + `Flags["discarded_0"]` increment, **`TestActivationCostDiscard_RoutesMadnessReplacement`** is the load-bearing §702.34a pin, canonical `Kind="discard"` event still fires for showmatch); **`internal/gameengine/per_card/discard_canonical_routing_r60_test.go`** pins the 3 per_card sites driven directly via `InvokeETBHook` / `FireCardTrigger` (4 tests: Mox Diamond canonical routing, Emet-Selch Madness routing, Smellerbee bulk-hand Madness routing across mixed plain+madness hand, Mox Diamond canonical Kind for showmatch). The ward_alt_payment + per_card_batch_k sites are covered by the engine activation-cost test's shared `DiscardCard` invariant — same helper, same machinery, same rules-bug closure. Sibling sites with **DUPLICATE** `Kind="discard"` emits after already calling `DiscardCard` (`phases.go:912-922` cleanup-step hand-size discard, `keywords_misc.go:568-577` retrace PayFn, `keywords_batch3.go:256-265` retrace, `keywords_batch3.go:300-310` jump-start) are left alone in this PR — they emit per-card events for showmatch granularity and don't bypass the canonical helper, so they're not in the same load-bearing-bug class. Full per_card + engine root suites pass. |
| 2026-05-30 | Loki r60 Phase G / Aziza spell-copy fabrication | **ZoneConservation x34 — Aziza, Mage Tower Captain spell-copy handler aliased originating `*Card` pointer into §707.2 StackItem** — Loki r60 seed-42 5K sweep dominant residual after Phase F (#851 + #773 + #853), 34 fabrication hits across one game (2762 / Lash Out / `h1OGVR200056` / turns 44-60). Phase F 5K verify doc (`docs/zonecons-phase-f-5k-verify-r60.md` @ commit `07252406`) surfaced + queued as Phase G. Aziza built `&StackItem{Card: castCard, IsCopy: true}` and `gs.Stack = append(...)` — no DeepCopy, no MintSpellCopy. When the copy resolved, `stack.go:1312`'s §707.10 cease branch fired `MarkInstanceIDCeased(gs, item.Card.InstanceID)` on the SOURCE's ID, then the source card living in seat-N's hand/graveyard was flagged as fabrication on every subsequent invariant tick. Structurally worse than the 10 sites Phase F closed (those at least DeepCopy'd, sharing the ID via inheritance — Aziza shared the pointer outright). | Closed. Routed the copy through the canonical `gameengine.MintSpellCopy` chokepoint + `gameengine.PushStackItem` (matches the alania / zada / krark / mica / mendicant / rootha / kalamax / ivy / fire_lord_azula / ulalek pattern Phase F established). 7-line diff in `internal/gameengine/per_card/aziza_mage_tower_captain.go`. 4 new regression tests in `aziza_spell_copy_r60_test.go`: `TestAziza_SpellCopy_DistinctInstanceID` (structural — copy *Card is freshly minted, distinct InstanceID), `TestAziza_SourceIDSurvivesCopyResolution` (end-to-end leak shape — source's InstanceID survives §707.10 cease of the copy, exact game-2762 invariant fingerprint), `TestAziza_InsufficientCreatures_NoCopy` (cost-pay guard), `TestAziza_OpponentCast_NoCopy` (controller gate). Updated existing r51 test `TestAziza_R51_CopiesSpellOnInstantCast` which was pinning the broken contract (`top.Card == castCard`) to assert the new correct contract. Loki re-run `--games 5000 --seed 42 --invariant zone-conservation --nightmare-boards 0 --instanceid-strict-census`: 34 → **0** violations (verdict: CLEAN, 2m38s). Audit-rule callout (in `docs/zonecons-phase-g-aziza-r60.md`): any future per_card handler pushing `IsCopy=true` to `gs.Stack` MUST route through `MintSpellCopy` from inception. Handlers resolving copies via `InvokeResolveHook` directly (Isochron Scepter, Panoptic Mirror) are exempt — they don't push to the stack so §707.10 cease never fires. |
| 2026-05-30 | `dev/event-kind-normalize-wave2-r60` (this PR) | **Event-Kind normalize wave 2 — `untap` legacy Kind + bypassed `UntapPermanent` helper + silent §702.124 Inspired / §122.4 stun-counter rules-bug** — follow-up to wave 1 (PR #830 lose_game normalize). The canonical `UntapPermanent` helper at `internal/gameengine/keywords_inspired.go:134` was introduced as the mid-turn untap chokepoint that fires §702.124 Inspired triggers AND consumes §122.4 stun counters, but its own docstring flagged: *"the existing `perm.Tapped = false` direct-assignment callsites continue to work — they just won't fire inspired. Migration of those callsites is out of scope for this commit."* Audit found 4 high-impact sites still bypassing the helper, all emitting non-canonical `Kind="untap"`: (a) the engine's `resolveUntap` at `resolve.go:1582-1594` — THE canonical AST-resolution path for every parsed `Untap target permanent` oracle phrase (touches every "Untap target X" instant/sorcery + every activated-ability untap); (b) `per_card/palinchron.go:54-65` (ETB untaps 7 lands); (c) `per_card/peregrine_drake.go:54-65` (ETB untaps 5 lands); (d) `per_card/dramatic_reversal.go:45-56` (resolve untaps all nonland — the Isochron Scepter combo path). Dramatic Reversal carried a load-bearing rules bug: it untaps creatures, so Inspired-bearing creatures (Pain Seer, Felhide Spiritbinder, Fanatic of Xenagos, etc.) untapped by an Isochron Scepter+Dramatic Reversal loop did NOT fire Inspired — silently broken since the helper landed. The engine `resolveUntap` path had an even broader gap: every parsed "Untap target creature" effect bypassed Inspired AND stun counters (Brine Elemental / Hex Parasite). | Closed. All 4 sites refactored to route through `gameengine.UntapPermanent(gs, p, "<reason>")` which emits canonical `Kind="untap_done"` with the reason in Details, consumes §122.4 stun counters via the existing `stun_counter_removed` emit, and fires §702.124 Inspired via `FireInspiredTriggers` on creatures. Counter loops use the helper's bool return to track success (`if UntapPermanent(...) { untapped++ }`). Per-handler per_card `emit(slug, ...)` calls are preserved as card-specific instrumentation distinct from the generic untap event. 11 regressions across two test files: **`internal/gameengine/untap_event_kind_normalize_r60_test.go`** pins the engine-side resolveUntap path (5 tests: canonical `untap_done` Kind, §702.124 Inspired fires, §122.4 stun consumed instead of untap, already-untapped target no-ops, legacy `untap` Kind fully retired); **`internal/gameengine/per_card/untap_event_kind_normalize_r60_test.go`** pins the 3 per_card sites (6 tests: Palinchron/Peregrine/Dramatic Reversal each emit canonical Kind, **`TestDramaticReversal_FiresInspiredTrigger`** is the load-bearing rules-bug pin, Dramatic Reversal and Palinchron both respect stun counters). Audit of remaining `Tapped = false` sites (37 more across engine + per_card) is in scope for wave 3 — additional Inspired-blind paths (Aggravated Assault, Najeela, Insurrection, Kumena, etc.) — but this PR keeps scope tight at the 4 highest-traffic sites; `resolveUntap` alone covers every parsed "Untap target permanent" oracle phrase, so the AST-driven surface is fully canonicalized in one shot. Full per_card + engine suites pass. |
| 2026-05-30 | `dev/per-card-lose-game-event-kind-normalize-r60` (this PR) | **lose_game event-Kind normalization follow-up + atemsis ordering bug** — the 2026-05-30 row immediately below closed the `MarkSeatLostByEffect` extraction across 8 per_card card-effect sites, with this follow-up flagged as: *"Each card has its own log Kind ('seat_lost', custom shapes) — a follow-up sweep should normalize to the canonical `lose_game` event."* Audit on this branch found 2 of the 8 still emitting non-canonical duplicates on top of the helper's canonical `lose_game` Event: `atemsis_all_seeing.go:73` emitted `Kind="player_loses"` and `demonic_pact.go:98` emitted `Kind="seat_lost"`. Both carried the same Seat + Source + reason payload as the canonical event — pure audit-noise duplicates. The Atemsis audit also surfaced a deeper **load-bearing ordering bug**: the handler called `emitWin` BEFORE `MarkSeatLostByEffect`, which made the helper short-circuit at its `if s.Lost { return false }` guard (emitWin's opponent-lockstep had already direct-set the defender's `Lost=true`), silently bypassing §614 `would_lose_game` — Platinum Angel was completely inert against Atemsis pre-fix. | Closed. Dropped both duplicate `LogEvent` calls; encoded the previously-duplicate `Details["reason"]` content into the helper's `srcSourceName` argument so the canonical `lose_game` Event carries the same audit detail in its `Source` field (e.g. `"Atemsis, All-Seeing — six distinct mana values among cards in hand"`, `"Demonic Pact (final mode — all three other modes exhausted)"` — mirrors Frodo's existing `"Frodo, Sauron's Bane — Rogue combat damage with Ring level 4"` pattern). Flipped the Atemsis order: `MarkSeatLostByEffect` first, `emitWin` only if the helper returned `applied=true`. This preserves §614 semantics — Platinum Angel now correctly cancels the Atemsis loss, and the controller does NOT win when the cancel fires. 5 regressions in `internal/gameengine/per_card/lose_game_event_kind_normalize_r60_test.go`: Demonic Pact final-mode → exactly 1 `lose_game` Event with mode detail in Source + 0 `seat_lost`; Atemsis 6+ distinct → exactly 1 `lose_game` with mechanism detail + 0 `player_loses`; below-threshold Atemsis + non-final Demonic Pact emit zero loss events (defends over-normalization); **`TestAtemsis_PlatinumAngelCancelsLoss`** is the load-bearing pin on the order-flip (mirrors PR #770's Triskaidekaphobia cancel pattern — defender survives, controller doesn't win, exactly 1 `lose_game_replaced` + 0 `lose_game` + 0 `per_card_win`). Sba.go's `seat_lost` Kind for CR §104.4b mandatory-loop draws is a different semantic and intentionally left alone. Audit verified the other 6 migrated handlers (angel_of_destiny, etrata, frodo, pact_cycle, pact_of_negation, sanguine_exquisite) emit zero non-canonical loss events. Full per_card suite + engine root suite pass. |
| 2026-05-30 | Loki re-verification / ZoneCastGrantExpiry third cross-validation pass | **ZoneCastGrantExpiry (8) — impulse-play / cast-from-exile grant outlived its declared expiry** — original 2026-05-19 Loki r41 cluster (8 hits on seed 41), drilled down through the r60 partial-fix landscape (PR #106 source-LTB family / PR #209 per_card Duration stamps + same-turn FP / PR #554 heist + may_play_exiled_free Duration+GrantTurn stamps / commit `72b95136` `ExpireSourceGrants` in `HandleSeatElimination` / PR #582 §514.2 phase-boundary verification / 2026-05-27 first formal closure verification on 3 seeds = 11K chaos + 20K nightmare). | Closure re-confirmed on fresh `origin/main` 2026-05-30 — a third independent verification pass on the original r41 cluster seed. Loki `--games 5000 --seed 42 --invariant zone-cast-grant-expiry --nightmare-boards 0` → **0 violations / 0 crashes / 5000 clean games** (2m08s, 39 g/s). Full `go test ./internal/gameengine/... -count=1 -timeout 300s` green. Lifecycle coverage audit confirms every grant-creation site is fully plumbed with the canonical triple (`Duration=until_end_of_turn` + `GrantTurn=gs.Turn` + `SourceTimestamp=src.Timestamp`): structured `impulse_play` (`resolve_helpers.go:1503`), `heist` (`resolve_helpers.go:380`), `may_play_exiled_free` (`resolve_helpers.go:602`), `resolveResidualByText` impulse_play branch (`resolve_helpers.go:4763`, with `reResPlayAsLongAsExiled` correctly omitting the EOT clock per CR — cast consumes the grant via `RemoveZoneCastGrant`). Every cleanup path reclaims: source-LTB via `ExpireSourceGrants` (all 6 battlefield-exit paths + `multiplayer.go:440` seat-elim sweep), EOT via `ExpireZoneCastGrants` in `phases.go` cleanup, game-end via `multiplayer.go:294` flush on `Flags["ended"]=1` transition. No live gap remains. No code change shipped under this row — purely a third-pass closure confirmation on the long-running r41-era cluster so future workers don't re-investigate from the 2026-05-19 baseline. |
| 2026-05-30 | Issue-log currency sweep (`dev/issue-log-currency-sweep-r60`) | **Open issues table walked end-to-end — confirmed empty, all rows currently live in Resolved** | Audit pass against `CLAUDE.md` Open table on fresh origin/main: zero live rows under the header (only the column row `\| Date \| Source \| Issue \| Severity \| Notes \|`). Spot-verified the 5 most-likely-stale Resolved rows for accuracy: (a) **Iroh, Grand Lotus TLA flashback-grant** — handler at `internal/gameengine/per_card/gen_iroh_grand_lotus.go` + 8 regressions in `iroh_grand_lotus_test.go` all PASS on current main (`go test ./internal/gameengine/per_card/ -run 'TestIroh_'`); row stays closed. (b) **`removePermanent` API-misuse sweep** — `grep '\.Lost = true' internal/gameengine/per_card/**/*.go` yields only `helpers.go:84` (intentional `emitWin` opponent-lockstep); etrata/bilbo/thassa all routed through `ExilePermanent` per the 2026-05-30 row above. (c) **Era 2 scaffold audit** — re-ran `python3 scripts/era2_scaffold_audit.py` clean at 75/75 + 408/408 (0.0% gap both). (d) **Era 3 scaffold audit** — re-ran clean at 54/55 condition + 538/538 trigger (lone "if they do" parser shape, not a missing scaffold). (e) **4,190 unbucketed scaffold issue** — updated the 2026-05-24 row immediately above with fresh aggregate (348 unbucketed across 18,152 nodes, **1.9% overall gap, -91.7% from baseline**). No new issues surfaced; no engine code changed in this sweep — pure audit-currency doc update. |
| 2026-05-30 | structural-analysis-r60 Wave 1 (PR #5 + retrospective audit) | **9 per_card direct-Lost setters bypass canonical `resolveLoseGame` pipeline** — original 2026-05-28 row listed `angel_of_destiny.go:143`, `atemsis_all_seeing.go:70`, `demonic_pact.go:95`, `etrata_the_silencer.go:68`, `frodo_saurons_bane.go:144`, `pact_cycle.go:259`, `pact_of_negation.go:96`, `sanguine_exquisite.go:38`, `batch_aj_win_the_game.go:344`, plus the intentional `emitWin` opponent-lockstep at `helpers.go:84`. | Closed end-to-end. The 2026-05-29 `MarkSeatLostByEffect` extraction (`resolve.go:2982`) refactored 7 of the 8 card-effect sites to route through the canonical §104.3e chokepoint (with §614 `would_lose_game` replacement chain + `LostByEffect` stamp + canonical `lose_game` audit event). The lone holdout `batch_aj_win_the_game.go:344` (Triskaidekaphobia's "any seat at exactly 13 life loses" state-trigger) closed by **Wave 1 PR #5** (`dev/structural-wave1-pr5-batch-aj-platinum-angel-r60`, 2026-05-30) — now routes through `MarkSeatLostByEffect`, with a new `TestTriskaidekaphobia_PlatinumAngelCancelsLoss` regression pinning the end-to-end fix (seat at 14 life with Platinum Angel does NOT lose when Trisk picks lose-1 mode). The `helpers.go:84` `emitWin` opponent-lockstep is the intentional §104.2c win path (NOT a card-effect loss bypass) — left alone. Verified 2026-05-30: `grep "\.Lost = true" internal/gameengine/per_card/**/*.go` yields only `helpers.go:84`. Audit-rule callout: any future per_card "set Lost directly" pattern should be routed through `MarkSeatLostByEffect` from inception. |
| 2026-05-30 | structural-analysis-r60 Wave 1 / removePermanent sweep | **`removePermanent` API-misuse sweep — etrata x2 + bilbo x1 + thassa x1 (4 sibling sites)** — original 2026-05-25 row recorded the sweep as "4 of 5 sibling sites still standing" with the caveat "not crashing post-`b348f4a`... correctness-improvement work, not a fire". | Closed. Verified 2026-05-30 via structural analysis (`docs/structural-analysis-r60.md` §2 + cross-check on this branch): `etrata_the_silencer.go:61` calls `gameengine.ExilePermanent(gs, target, perm)`, `thassa_deep_dwelling.go:77` calls `gameengine.ExilePermanent(gs, pick, perm)`, `gen_bilbo_birthday_celebrant.go:117` calls `gameengine.ExilePermanent(gs, src, src)`. The lone `removePermanent` reference in `etrata_the_silencer.go:81` is a comment-string from the legacy pattern, not a live call site. All four sites now route through the canonical §614-aware exile API (handles `would_be_exiled` replacement, §903.9b commander redirect, `DetachAll` for auras/equipment, and Replacement/ContinuousEffect unregistering). No code change shipped under this row — purely a stale-callout retirement, recorded so the gap-tracking page stays trustworthy. |
| 2026-05-27 | Loki r60 closure-verification / ZoneCastGrantExpiry | **ZoneCastGrantExpiry closure verified — full r60 surface is bit-stable clean across 3 seeds.** Era prior partial-fix landscape: PR #106 (r60 round 1) closed the dominant source-LTB + game-end family (-90% to 4 residuals); PR #209 stamped 5 missing per_card grant Durations + same-turn FP fix; PR #554 (`dev/zonecast-expiry-r60`) added impulse-play / heist / may_play_exiled_free Duration stamps + GrantTurn timestamps to the `NewFreeCastFromExilePermission` arms in `resolveModificationEffect`; commit `72b95136` added `ExpireSourceGrants` to `HandleSeatElimination`'s permanent-sweep loop (the 2026-05-24 seed-42/271828/31415 8-violation cluster); PR #582 (`dev/zonecast-cleanup-real-r60`) end-to-end-verified the impulse-play cleanup at §514.2 phase boundary. **2026-05-27 verification: re-ran Loki on fresh origin/main at the original r41 cluster's seed and two cross-validation seeds.** `--games 5000 --seed 42 --invariant zone-cast-grant-expiry --nightmare-boards 0`: **0 violations / 0 crashes / 5000 clean games.** `--games 3000 --seed 43`: 0 violations. `--games 3000 --seed 31415 --nightmare-boards 20000`: 0 chaos violations + 0 nightmare violations. Aggregate **11,000 chaos games + 20,000 nightmare boards across 3 seeds = 0 ZoneCastGrantExpiry violations.** The r41 baseline (1,652 violations on seed 42) is now genuinely closed. The original Loki r60 round-2 doc had projected 4 residuals (Chitinous Crawler + Illusionary Mask family with `sourceTimestamp=0`); the PR #209 same-turn-FP fix + Duration stamps absorbed those. No code change in this verification — purely an empirical closure of a long-running r60 issue cluster that had been driven down to "presumed clean" via incremental fixes without a consolidated re-run at original cluster depth. |
| 2026-05-26 | Era 2 scaffold audit re-verification | **Era 2 scaffold gap re-confirmed at 0% on 2026-05-26 corpus** — `python3 scripts/era2_scaffold_audit.py` reports 75/75 condition nodes and 408/408 trigger nodes bucketed across 537 Era 2 cards. Audit script exists (shipped via PR #96) and runs clean. No-op verification — no scaffold work remaining on Era 2. Leverage for future sweeps remains Era 3 (20% gap) and Era 1 longtail (15.8% gap) per the 2026-05-24 corpus-audit row. |
| 2026-05-25 | Loki r60 extended-seeds / seed 2718 game 3428 | **WinCondition x2 false-positive — "seat 0 lost via poison but has only 2 poison counters (< 10)"** (turn 39 end_of_combat; pod: Genevieve Conniving Dragon / Bartolomé del Presidio / Zndrsplt / Hapatra Vizier of Poisons). Initial hypothesis: `LossReason` mis-attribution by a non-poison loss path. | Root cause was a **invariant false positive**, not a `LossReason` bug. Seat 0 legitimately took 10+ poison counters from Hapatra's infect package (Blightwidow / Dakmor Scorpion / Broodguard Elite); SBA 704.5c fired correctly with `LossReason="ten or more poison counters (CR 704.5c)"`. Then a post-loss effect (Hapatra-pod counter-swap, "remove all poison counters" target effect, etc.) decremented seat 0's `PoisonCounters` to 2. Per CR §704.5c the loss is permanent — counter adjustments after Lost=true are state-preserved for audit but don't undo. The invariant's substring-match against `LossReason` re-validated against CURRENT counter state and false-positived. **Fix:** added `!s.LeftGame` guard to both the poison check AND the commander-damage check, mirroring the existing guard on the `life` check ("Post-elimination triggers can modify state afterward — only flag if the seat hasn't been cleaned up"). Once `LeftGame=true`, trust the loss reason. 5 regressions in `wincondition_post_elim_r60_test.go`: post-elim poison reduction passes (bit-stable seed-2718 shape); pre-LeftGame low-poison still fires (defends over-narrowing); legitimate poison loss passes; commander-damage equivalent (post-elim passes); commander-damage pre-LeftGame still fires. Loki re-run `--games 3430 --seed 2718 --invariant win-condition`: 2 → **0** violations. |
| 2026-05-25 | Loki r60 deep-stress / seed 271828 game 4773 | **ReplacementCompleteness x1 — Rest in Peace replacement skipped on Rapier Wit graveyard entry** (turn 21 cleanup; flagged 2026-05-24 deep-stress, fixed 2026-05-25). Original hypothesis: a non-creature-death zone-change (discard, sacrifice-non-perm, tutor-to-graveyard) was skipping `FireGraveyardEvent` and thus the RIP redirect. | Root cause was **invariant false positive**, not a missing replacement plumb. Event log: Rapier Wit `zone_change → graveyard` at event 585, Rest in Peace `cast` + `enter_battlefield` at events 588/594. RIP wasn't on the battlefield yet when Rapier Wit died — the replacement was correctly inert per CR §614.6, but `checkReplacementCompleteness`'s existing destroy-event guard only covered cards passing through a preceding `destroy` event. Spells resolving from the stack directly into the graveyard (CR §608.2g) have no `destroy`, so the guard didn't fire on a clean game. **Fix (commit `9f801ed`):** before flagging, scan forward in the event window for an `enter_battlefield` of Rest in Peace / Leyline of the Void. If the exile effect's ETB index is later than the offending zone_change, the effect wasn't on the battlefield when the card went to graveyard — no replacement could have applied. Skip. 3 regressions in `replacement_completeness_rip_etb_fp_r60_test.go`: RIP ETB after zone_change (FP suppressed); RIP ETB before zone_change (still flags real misses, defends against over-narrowing); Leyline of the Void equivalent. Loki re-run `--games 4775 --seed 271828`: 1 → **0** violations (was the only chaos hit on that seed at 5K depth). |
| 2026-05-25 | Loki r60 extreme-stress / seed 31415 game 9111 | **SBACompleteness x6 — seat 0 life=0 Lost=false, no loss-prevention, SBA 704.5a missed** (turn 57 cleanup; pod: Grenzo Havoc Raiser / Spider-Man 2099 / Inga and Esika / Eron the Relentless). Recent events showed `replacement_applied source=Platinum Angel` firing twice on seat 0, but NO Platinum Angel was on any seat's battlefield in the state snapshot — the replacement effect was PHANTOM, perma-preventing seat 0 from losing. | Root cause: a stale Platinum Angel `would_lose_game` cancel-handler in `gs.Replacements` whose `SourcePerm` had left the battlefield without an `UnregisterReplacementsForPermanent` call. Every CANONICAL leave-play path (DestroyPermanent / ExilePermanent / sacrificePermanentImpl / BouncePermanent / destroyPermSBA / sacrificePermSBA / HandleSeatElimination) calls the unregister, but the codebase has several non-canonical paths that use `removePermanent` directly without it: mutate-eaten perm at `keywords_batch6.go:293/306`, sweep alt-cost return at `keywords_sweep.go:196`, per_card flicker/blink/exile-self helpers (Brago, Displacer Kitten, Emiel, Deadeye Navigator, Etrata, etc.). Rather than hunt each leak individually, added a **defensive backstop in `pickReplacement`**: any replacement whose `SourcePerm != nil` AND is not on any seat's battlefield is now silently skipped. Real global replacements (`SourcePerm == nil`, e.g. Anointed Procession-style doublers) bypass the check. Phased-out permanents (CR §702.26) count as "on the battlefield" since phasing doesn't leave play. New helper `permIsOnAnyBattlefield` colocated in `replacement.go`. 4 regressions in `replacement_stale_source_r60_test.go`: phantom Platinum Angel rejected; live Platinum Angel still cancels; global nil-source replacements still apply; end-to-end "registered then left play without unregister" path covered. Loki re-run `--games 10000 --seed 31415 --invariant sba-completeness`: 6 → **0** violations. |
| 2026-05-25 | Loki r60 extreme-stress / seed 99 game 9804 | **ResourceConservation x2 — "seat 0 is Lost but has ManaPool=1"** (turn 42 end_of_combat). Initially hypothesized as a Gyome / Food-token lifecycle gap; root cause turned out to be elsewhere. | Root cause: Myr Moonvessel's "When this dies, add {1} to your mana pool" triggered ability fired in the same SBA window as seat 0's elimination. Event trail: `[1872] sba_704_5a → s.Lost=true`, `[1873-1874] Myr Moonvessel destroyed`, `[1876] triggered_ability` (queued in `gs.pendingTriggers` — CR §603.3b batch), `[1877] seat_eliminated` (HandleSeatElimination clears mana to 0), batch flushes, `[1878] stack_push` + resolve, `[1883] add_mana seat=0 +1` — invariant fires. **Fix:** `HandleSeatElimination` previously purged `gs.Stack` items controlled by the leaving seat (step 2) but missed `gs.pendingTriggers` — the CR §603.3b batch of abilities that have triggered but not yet been pushed. Added a "step 2b" mirror that purges `gs.pendingTriggers` for items where `Controller == seatIdx`, emitting a `pending_triggers_purged_on_leave` event for auditors. Same CR §800.4a semantics: abilities of a leaving player cease to exist, whether they're on the stack OR in the pending batch. 4 regressions in `seat_elimination_pending_triggers_r60_test.go`: bit-stable Myr Moonvessel replay (purges seat-0 trigger), preserves seat-1 triggers (defends against over-broad sweep), audit event emitted with right Seat/Amount, no-op when no pending triggers from leaving seat. Loki re-run `--games 9810 --seed 99 --invariant resource-conservation`: 2 → **0** violations. |
| 2026-05-25 | Loki r60 extreme-stress / seed 1337 game 8921 | **CardIdentity x40 — single deep leak persisting 8-10 turns** (turn 41 cleanup; pod: Magda / Shiko / Spider-Gwen / Zidane). Highest-multiplier residual of the 10K-depth sweep. Bisection through the three handlers identified Zidane as the source. | Root cause: Zidane, Tantalus Thief's ETB control-steal registers a `next_end_step` delayed trigger to return the stolen creature. The pre-fix `EffectFn` blindly re-added the captured `*Permanent` to the original owner's battlefield with no check for whether the perm had LEFT play (died → graveyard, exiled, bounced). When the captured creature died during the turn, its `*Card` moved to graveyard, and the EOT closure then re-added the (now-stale) `*Permanent` to the original owner's battlefield. The underlying `*Card` was in both graveyard AND battlefield → same-pointer-in-two-zones, persisting until the perm was destroyed again. Per CR §611.3c, when a control-changing effect ends, control reverts to the owner ONLY IF the permanent is still on the battlefield — otherwise the duration's expiry is a no-op. **Fix:** added a battlefield-presence scan to the Zidane EOT closure in `gen_zidane_tantalus_thief.go`; if the captured perm is NOT on any seat's battlefield, the closure clears the lifelink/haste flags (so they don't ride along with a future reanimation) and returns without re-adding. Same defensive pattern as Adric / Oketra / Gisa / Athreos / The Reaper §704.6d. 3 regressions in `zidane_eot_dead_target_r60_test.go`: died-then-EOT no-ops (bit-stable seed-1337 game-8921 shape); exiled-then-EOT no-ops; alive-at-EOT still successfully returns (defends against over-narrowing). Loki re-run `--games 8925 --seed 1337 --invariant card-identity`: 40 → **0** violations. |
| 2026-05-24 | Loki r60 extreme-stress / seeds 42 + 31415 + 271828 | **ZoneCastGrantExpiry x8 across 3 seeds (3 distinct games)** — dominant residual signature at 10K depth. Seed 42 game 5480: `grant for "Compound Fracture" (zone=graveyard duration=while_source_on_bf sourceTimestamp=32 source=Kess, Dissident Mage)`; seed 271828 game 5399 turn 50: similar with Gonti Canny Acquisitor pod; seed 31415 game 5610 turn 33: different pod, same shape. | Root cause: `HandleSeatElimination` at `multiplayer.go:344-356` (the CR §800.4a seat-loss cleanup path) removed the leaving seat's permanents from every battlefield and called `UnregisterReplacementsForPermanent` + `UnregisterContinuousEffectsForPermanent`, but did NOT call `ExpireSourceGrants(gs, p.Timestamp)`. PR #106's earlier r60 plumbing covered every OTHER LTB-equivalent path (`DestroyPermanent` / `ExilePermanent` / `sacrificePermanentImpl` / `BouncePermanent` / `destroyPermSBA` / `sacrificePermSBA`) — the seat-elimination path was the lone gap. When Kess decked out and was eliminated, his `while_source_on_bf` graveyard-cast grant on Compound Fracture survived past the source. **Fix:** added `ExpireSourceGrants(gs, p.Timestamp)` to the permanent-sweep loop in `HandleSeatElimination`, scoped to the same condition that already unregistered Replacements/ContinuousEffects (`p.Controller == seatIdx \|\| p.Owner == seatIdx`). 4 regressions in `seat_elimination_grant_expiry_r60_test.go`: bit-stable Kess+Compound-Fracture replay; preserves grants from surviving seats (defends against over-broad sweep); covers Gilded-Drake control-trade case (controller==leaving seat but owner!=leaving); no-op when leaving seat has no grant-source permanent. Loki re-runs `--invariant zone-cast-grant-expiry`: seed 42 (`--games 5485`) 2 → **0**, seed 271828 (`--games 5410`) 4 → **0**, seed 31415 (`--games 5620`) 2 → **0**. All three seeds in the cluster closed with the same fix. |
| 2026-05-24 | Loki r60 deep-stress / seed 2024 game 2798 | **CardIdentity x24 — Athreos, Shroud-Veiled cross-seat reanimate race** (turn 36 cleanup; Woodland Liege + Athreos appearing simultaneously on seat 2 AND seat 3 battlefields). Event log: both Athreos handlers fired on the same creature_dies event, both claiming the dying *Card. | Root cause: when TWO Athreos controllers had each placed a coin counter on the dying creature, both creature_dies triggers fired in sequence. The first handler pulled the *Card from owner's graveyard via `createPermanent`'s zone sweep and placed a Permanent on its controller's battlefield. The second handler then raced — the *Card was no longer in graveyard, but `createPermanent`'s dedup at `helpers.go:299-303` only checks the TARGET seat's battlefield, so the race-loser still wrapped the already-stolen *Card in a fresh Permanent on its own seat. Same race surface as the Adric / Oketra / Gisa / The Reaper §704.6d fixes earlier in r60. **Fix:** `athreosShroudDies` now scans owner's graveyard for the dyingCard before delegating to `enterBattlefieldWithETB`; if absent, a sibling Athreos won the race → emits `card_already_stolen` partial and no-ops. Mirrors the established Gisa pattern at `gisa_glorious_resurrector.go:65-82`. 3 regressions in `athreos_cross_seat_race_r60_test.go`: race-loser no-ops in both seat orderings, single-Athreos path unaffected (defends against over-narrowing). Loki re-run `--games 2800 --seed 2024`: 24 → **0** violations. |
| 2026-05-24 | Loki r60 deep-stress / seed 2025 game 3180 | **SBACompleteness x1 — Charix, the Raging Isle survives at toughness=-7** (turn 59 end_of_combat, base 0/17, three stacked `+8/-8 until_end_of_turn` mods). Initially hypothesized as a self-referencing +X/-X stacking bug in the Charix activation handler. | Root cause was an invariant false positive, not a Charix handler bug. The three +8/-8 mods are rules-correct per CR §608.2b — each activation creates a separate continuous effect that stacks. The 3rd activation resolved AFTER the game ended in combat (seat 0 [WON], all opponents [LOST]); `StateBasedActions` at `sba.go:41` short-circuits on `gs.Flags["ended"] == 1`, so the §704.5f pass that would have moved Charix to the graveyard at toughness=-7 never ran. `checkStackIntegrity` already had this ended-skip (invariants.go:355); `checkSBACompleteness` was missing the mirror. **Fix:** added an `ended` early-skip to the creature-toughness loop of `checkSBACompleteness`, scoped narrowly — the life-loss arm continues to fire even at `ended=1`, preserving the cap-draw r60 regression pin (`sba_cap_draw_r60_test.go`'s counterfactual depends on it). 4 regressions in `charix_ended_sba_r60_test.go`: toughness check skipped post-ended, life check still fires post-ended (cap-draw counterfactual), toughness check still fires when live, `StateBasedActions` itself short-circuits post-ended. Loki re-run @ 3181 games / seed 2025: chaos 1 → **0**, nightmare 0 → **0**. |
| 2026-05-24 | Loki r60 mega-stress / nightmare boards (seeds 31415 + 271828) | **CardIdentity x4 across 2 seeds — Necrogen Communion *Card pointer appearing in both seat 2 graveyard and seat 2 stack on nightmare boards 9751, 8676, 4862, etc.** Initially suspected to be the same r41-era cluster as Adric / Oketra / Dread (genuine zone duplication via per_card handlers stealing already-relocated cards). | Root cause turned out to be an **invariant false positive**, not an engine zone-leak. Added per-board violation-details plumbing to `cmd/hexdek-loki/main.go` (mirroring the chaos-side `Violation Details` section so the nightmare phase emits Board / Invariant / Message / State per violation — addresses the "needs nightmare-phase per-board breadcrumb plumbing" follow-up from the original mega-stress doc) and bisected to the signature: Necrogen Communion (an Aura with no AttachedTo because the nightmare board skips `attachAuraOnETB`) is destroyed by SBA 704.5n, its *Card lands in seat 2's graveyard, and its `Triggered{event: "die"}` ability (parser approximation of "when enchanted creature dies") pushes onto the stack via `PushTriggeredAbility` in `stack.go:194-201`. That helper explicitly documents `item.Card = src.Card` as a log-label, not a zone occupant ("StackItem.Card is usually for spells, not triggers, but we point it at the source card so logs show the right name"). The CardIdentity invariant was counting that log-label as a real stack-zone reference, flagging the card as duplicated. **Fix:** `checkCardIdentity` now skips stack items where `item.Source != nil` OR `item.Kind` is "triggered" / "activated" — only spell items (Kind=="spell" + Source==nil) reflect a card actually moving through the stack zone per CR §405. Real card duplications across graveyard + stack from genuine spell items still fire the invariant. 4 regressions in `cardidentity_ability_stack_r60_test.go`: triggered ability log-label clean, activated ability log-label clean, real spell-item duplication still flagged (defends against narrowing too far), N ability items all referencing same source card stay clean. Loki re-run nightmare-only @ 10000 boards each: seed 31415: 2 → **0**, seed 271828: 2 → **0**. Full gauntlet wide-seed sweep is now bit-stable clean. |
| 2026-05-24 | Loki r60 mega-stress / seed 31415 game 237 | **TriggerCompleteness x2 false-positive — Gisa, Glorious Resurrector flagged as missing trigger on a SAME-CONTROLLER sacrifice** (Blackbloom Rogue sac'd via Birthing Ritual chain at turn 55 cleanup). Originally suspected to be the same race as PR #161 (cross-seat CardIdentity with §704.6d commander sweep) — turned out to be a separate, invariant-side issue, not an engine bug. | Root cause: `checkTriggerCompleteness` in `invariants.go:734-746` matched bearer-seat == death-seat without considering opp-only trigger handlers. Gisa's per_card handler at `gisa_glorious_resurrector.go:47` correctly early-returns on `controllerSeat == perm.Controller` (her oracle text is "if a creature an OPPONENT controls would die, exile it instead"), so when seat 0 sacrificed Blackbloom Rogue, Gisa's handler did nothing — but the invariant demanded a trigger event that correctly didn't fire. **Fix:** new `opponentOnlyCreatureDiesTriggers` map in invariants.go lists the 6 known per_card handlers that early-return on same-seat dies (Gisa, The Reaper King No More, Toxrill, Yahenni Undying Partisan, Grave Pact, Grave Betrayal — derived from a one-shot audit of every per_card `OnTrigger("...", "creature_dies", ...)` registration for the `controllerSeat == perm.Controller` early-return pattern). For these bearers the invariant now matches DIFFERENT-seat deaths (keeping the standard same-seat match for own-creature trigger bearers like Sefris, Pitiless Plunderer, Cruel Celebrant). 6 regressions in `trigger_completeness_opp_only_r60_test.go`: Gisa same-seat-sac passes, Gisa opp-death-no-trigger fires, Gisa opp-death-with-trigger passes, Sefris own-side path still works, Sefris opp-side ignored, all 6 opp-only bearers swept in a parametric test. Loki re-run @ 2000 games / seed 31415: chaos 2 → **0** violations (nightmare CardIdentity 2 unchanged — separate residual). NOT the same as PR #161 — that was a per-card handler race fix; this is an invariant filter gap. |
| 2026-05-24 | Loki r60 stress / seed 1337 game 465 | **SBACompleteness 10x + LifeConsistency 2x — seat 0 alive at life=0 for 5+ turns after a hidden SBA-cap mandatory-loop draw**. Originally hypothesized as a loop_shortcut → combat-damage SBA-skip; root cause turned out to be deeper. | The SBA-cap path in `sba.go:158-172` (CR §704.3 safety cap at 40 passes — typically tripped by Worldgorger Dragon + Animate Dead and similar mandatory loops) set `gs.Flags["ended"] = 1` and `gs.Flags["game_draw"] = 1` to flag a CR §104.4b draw, but did NOT mark any seats Lost. From that moment on, every subsequent `StateBasedActions` call short-circuited at `sba.go:41` (`if gs.Flags["ended"] == 1 { return false }`), but `CheckEnd` at `multiplayer.go:213-214` still gated on `len(alive) > 1` — and since no seats were Lost, alive count stayed at 4 and the turn loop kept calling `tournament.TakeTurn` with SBAs permanently muted. Combat damage subsequently took seat 0 to life=0 with no SBA pass to set Lost, surfacing as the "alive at life=0" invariant violation that persisted across turns 56-60. **Fix:** when the SBA cap fires, also mark every non-Lost seat as `Lost=true` with reason "mandatory loop draw (CR 104.4b via SBA cap)" so `LivingSeats()` drains to 0, `CheckEnd` returns true on the next call (simultaneous-elim draw branch), the turn loop terminates, and invariant scans see no alive-at-life-≤-0 seats. Per-seat `seat_lost` events emitted for auditors. 4 regressions in `internal/gameengine/sba_cap_draw_r60_test.go`: cap-draw marks all seats Lost; mirrors game-465 seat-0-at-life-0 + seat-2-at-life=-1 state and confirms both SBACompleteness and LifeConsistency invariants pass post-cleanup; pre-fix counterfactual where flags are set but seats not Lost confirms the invariants DO fire (pins root cause to the cleanup gap, not a separate engine bug); `ended=1` short-circuit at sba.go:41 still behaves correctly (helpers don't run after game ends). Loki re-runs @ 5000 games each: seed 42=0/0, seed 43=0/0, seed 99=0/0, seed 7=0/0, **seed 1337: 12 → 0** (-100%). All five seeds now fully clean. |
| 2026-05-24 | Loki r60 final / seed 43 game 1003 | **SBACompleteness — District Mascot survived as 0/0 creature for 52+ turns** (turn 53 upkeep, seat 1; base=0/0, counters=map[], mods=<none>). The lone residual from the two-seed final gauntlet (`docs/loki-r60-final-report.md`). | Root cause: the AST classifier emits "this creature enters with a +1/+1 counter on it" as a `Static` ability with `Modification.ModKind == "etb_with_counters"`, but the engine only handled `etb_with_counters` inside `resolveModificationEffect` — i.e. when the modification appeared as a resolving spell EFFECT. Static abilities don't resolve through the stack, so District Mascot's printed-0/0 self-replacement counter (CR §122.1g / §614.1d) was never applied. The creature entered at 0/0 and SBA 704.5f would eventually kill it, but the loki invariant scan caught the window before the next state-based pass. Fix: new `ApplyStaticETBCounters` primitive in `internal/gameengine/etb_static_counters.go` scans `perm.Card.AST.Abilities` for Static abilities with `etb_with_counters` ModKind and applies the args[0] count of args[1] counter kind. Wired into both ETB entry points: `resolvePermanentSpellETB` (stack-cast path, runs immediately after `ApplyRiot`) and `FirePermanentETBTriggers` (blink/reanimate/token-mint path, runs before the trigger cascade so observers see the perm in its final counter state). Honors face-down skip per CR §708.4. 6 regressions in `etb_static_counters_test.go` (counter applied, SBA-survives-with-fix, SBA-kills-without-fix counterfactual, custom counter kind via Args[1], face-down skip, blink-path integration via `FirePermanentETBTriggers`). Loki re-run @ 5000 games / seed 43: 1 → **0** violations, both phases clean. Engine now genuinely at 0 across both two-seed final gauntlet seeds (42 and 43). |
| 2026-05-24 | Loki r60 ROUND 2 | **R60 fuzz milestone — 5000 games / seed 42 baseline: 1652 (r41) → 402 (r44) → 52 (round 1) → 10 (round 2)** | Cumulative -1642 / -99.4% from r41 baseline. Round 1 → round 2 was -42 / -81%, driven by PR #106 (ZoneCastGrantExpiry source-LTB + game-end), PR #110 (per_card batchH dispatcher fixes), PR #124 (AttachmentConsistency residual), and the trigger-cap + invariant-filter pair for TriggerCompleteness. Per-cluster final state: ZoneCastGrantExpiry 4 / TriggerCompleteness 2 / CardIdentity 2 / CombatLegality 2 / AttachmentConsistency 0 / ZoneConservation 0. The residual 4 ZoneCastGrantExpiry hits are 2 signatures (Chitinous Crawler + Illusionary Mask), both `sourceTimestamp=0` — same shape as the goldilocks-r60 surface, so deterministic + stochastic gauntlets cross-validate the next fix target. Zero panics, zero recovers. Full report at `docs/loki-r60-round2-report.md`. |
| 2026-05-24 | Corpus Audit | **4,190 unbucketed condition/trigger nodes across all 4 eras** (original 2026-05-08 entry, "Era 1 biggest gap (3,281), Era 3 highest % (76.2%)") | All 4 era scaffold sweeps shipped: PR #87 (era 1 — 19 new condition scaffolds), PR #88 (era 3 — wire 13 dead r48 scaffolds + 7 new), PR #94 (era 4 — 19 condition scaffolds), PR #96 (era 2 — 19 trigger-event slugs + extended audit script with per-event bucketing). **Re-verified 2026-05-30** (`dev/issue-log-currency-sweep-r60`, all 4 era audits run against 31,963-card AST dataset on fresh origin/main): Era 1 — 2499 condition (167 ub, 6.7%) + 11548 trigger (47 ub, 0.4%); Era 2 — 75 condition (0 ub, **0.0%**) + 408 trigger (0 ub, **0.0%**); Era 3 — 55 condition (1 ub, 1.8%, the lone "if they do" parser-shape per the 2026-05-26 audit row) + 538 trigger (0 ub, **0.0%**); Era 4 — 514 condition (57 ub, 11.1%) + 2515 trigger (76 ub, 3.0%). **Aggregate: 18,152 nodes, 348 unbucketed (1.9% overall gap) — down from the 4,190 baseline (-3,842 / -91.7%)**, and notably much better than the stale 14.8% snapshot this row originally recorded (which itself was a single-era condition-only metric, not the full aggregate). Leverage today: Era 4 condition longtail (57 cards, 11.1% — biggest % gap) and Era 1 condition longtail (167 cards, 6.7% — biggest absolute count, but dominated by `if`/`conditional` parser fallthroughs not missing engine scaffolds). Era 2 + Era 3 trigger surface remain at 0% — no scaffold work needed. |
| 2026-05-24 | Loki r60 round 1 residual | **ZoneCastGrantExpiry source-LTB + game-end family — Cruelclaw / Yawgmoth's Agenda / Narset Enlightened Master grants survived their source's LTB or a mid-combat game-end** (PR #106 `dev/zonecast-residual-deep-r60`) | Two distinct lifecycle bugs surfaced as 42 of 52 round-1 hits. (1) `while_source_on_bf` grants outlived their source permanent: `ExpireSourceGrants` existed at `zone_cast.go:681` but was never called from any LTB path — Yawgmoth's Agenda dies, its play-from-graveyard grant on Huatli's Final Strike survives until next EOT cleanup. Hooked `ExpireSourceGrants(gs, perm.Timestamp)` into all four LTB paths (`DestroyPermanent`, `ExilePermanent`, `sacrificePermanentImpl`, `BouncePermanent` in `zone_change.go`) and into `destroyPermSBA` + `sacrificePermSBA` in `sba.go`. (2) `until_end_of_turn` grants survived a mid-combat game-end: heist (Cruelclaw) and Narset-exile-cast grants registered on turn N had their EOT cleanup skipped when the game ended in combat phase before reaching cleanup (last opponent dies to lethal combat damage). Added a defensive `ZoneCastGrants` flush in `CheckEnd` at the `Flags["ended"]=1` transition — the grants can no longer be exercised legally once the game has ended. 5 regressions in `internal/gameengine/zonecast_residual_r60_test.go` pin each path (game-end purge, destroy/exile/bounce/sacrifice LTB). Loki r60 round 2 @ 5000 games / seed 42: ZoneCastGrantExpiry 42 → **4** (-90%). Residual 4 are the Swamp / Chitinous Crawler + Illusionary Mask family with `sourceTimestamp=0`, tracked separately. |
| 2026-05-30 | Corpus Audit — Era 4 verification | **Era 4 19-scaffold sweep is genuinely closed — re-dispatch under `dev/era4-scaffold-sweep-r60` confirmed all 19 already shipped** | Worktree dispatch ran `python3 scripts/era4_scaffold_audit.py` against fresh main: **Era 4 has 514 Condition nodes (457 bucketed, 57 unbucketed = 11.1% gap)** and **2515 Trigger nodes**. The 57 unbucketed are all `conditional` × 33 + `if` × 24 fallbacks from 1-of card-specific phrases (Hot Pursuit, Hixus, Search for Survivors, Sphinx Ambassador, Treasure Chest, Primal Surge, Kenessos, Pious Kitsune, Sarcomancy, Y'shtola Rhul, Faramir Field Commander, Spider-UK, etc.) with no clusterable pattern of ≥5 cards — none of them are part of the 19-scaffold set the original PR #94 promised. Cross-checked the 19 in three places: (1) `cmd/hexdek-thor/conditional_setup.go` lines 2353-2436 — all 19 cases present in `detectConditionScaffold` switch (`opponent_more_creatures`, `wasnt_creature_type`, `all_lands_type`, `was_attacking`, `permanent_mv_le`, `was_cast`, `didnt_die`, `self_didnt_etb_this_turn`, `no_named_tokens`, `wasnt_sacrificed`, `counters_on_self_ge`, `no_damage_since_last_turn`, `first_time_resolved_this_turn`, `player_no_creatures`, `another_typed_etb_this_turn`, `creature_etb_last_turn`, `exiled_with_count_ge`, `exiled_this_turn`, `damaged_creature_died`); (2) setup helpers at lines 6606-7069 (all 19 with description strings + flag stamping); (3) `cmd/hexdek-thor/era4_r60_scaffolds_test.go` (430 lines, 22 cases pinning all 19 `condScaffold*` kinds + 3 raw-text variants — `go test -run TestDetectConditionScaffold_Era4R60` passes clean). Audit script `scripts/era4_scaffold_audit.py` `BUCKETED_KINDS` (lines 84-95) already includes all 19 + synonym aliases. **No code change shipped under this audit row — purely a stale-callout correction matching the 2026-05-26 Era 3 precedent immediately below.** Future dispatches should not attempt "implement the 19 Era 4 scaffolds" — they shipped in PR #94 (recorded in the 2026-05-24 corpus-audit row above) and have been bit-stable since. If condition-scaffold work is wanted, Era 1 (202 unbucketed conditions) is the only era with a clusterable longtail still standing; Era 4's 57 is genuinely a 1-of card-shape distribution that needs per-card per_card handlers or AST parser improvements, not new scaffold Kinds. |
| 2026-05-26 | Corpus Audit — Era 3 verification | **Era 3 scaffold gap is genuinely closed — earlier "76.2%" / "20%" callouts were stale** | Two consecutive worktree dispatches (`dev/era3-scaffold-r60`, `dev/era3-scaffold-real-r60`) re-ran `scripts/era3_scaffold_audit.py` against fresh main. Result: **Era 3 has 55 Condition nodes total (54 bucketed, 1 unbucketed = 1.8% gap)** and **538 Trigger nodes (0 unbucketed = 0.0% gap)**. The lone unbucketed condition is on Reservoir Kraken: the parser splits the "if they do, tap this creature and create a 1/1 fish token" continuation into a standalone `Static{condition:conditional}` node continuing the prior `phase`-triggered effect — it is a parser-side framing artifact, not a missing scaffold Kind, and `grep -c "if they do" data/rules/ast_dataset.jsonl` shows 34 cards with the same oracle phrase that the parser merges into the parent effect properly. Compared to the other eras (Era 1: 8.1% gap / 202 unbucketed conditions — biggest absolute longtail still; Era 2: 0.0% clean; Era 4: 11.1% gap / 57 unbucketed conditions), Era 3 is the cleanest non-zero era and ranks 3rd-of-4 by gap %. Cross-checked the audit script's `BUCKETED_KINDS` (113 entries, ending at `dragon_beheld`) against the actual `detectConditionScaffold` switch in `cmd/hexdek-thor/conditional_setup.go` lines 2009-2400+: the engine implements **more** kinds than the audit whitelists (landfall / you_descended_this_turn / it_was_a_creature / no_creatures_on_battlefield / tribute_not_paid / bargained / shares_creature_type / not_their_turn / more_life_than_opponent / no_time_counters / wasnt_cast / lost_life_last_turn / damage_dealt_to_self_this_turn / more_cards_in_hand_than_opponents / self_is_untapped / self_has_no_counter / surge_cost_paid / library_empty / colored_mana_spent / self_is_suspended / life_above_starting / life_vs_half_starting / opponent_more_life / wasnt_blocking / opponent_more_creatures / wasnt_creature_type / all_lands_type / was_attacking / permanent_mv_le / was_cast / didnt_die / self_didnt_etb_this_turn / no_named_tokens / wasnt_sacrificed — 34+ kinds present in engine but not in audit whitelist), meaning **the audit conservatively over-reports the gap**, not under-reports. The "76.2%" snapshot from the 2026-05-08 corpus audit was retired by PR #88 (era 3 — wire 13 dead r48 scaffolds + 7 new) and further closed by the r60 sweep additions in `BUCKETED_KINDS` lines 104-112 (`discarded_nonland`, `control_n_tapped_creatures`, `gained_n_life_this_turn`, `drawn_n_cards_this_turn`, `starting_player`, `quest_counters`, `dragon_beheld`). **No code change shipped under this audit row — purely a stale-callout correction.** Future dispatches should not attempt "implement top 5 Era 3 unbucketed Kinds" — there is exactly 1 unbucketed Era 3 Kind and it is a parser shape, not a scaffold gap. If condition-scaffold work is wanted, Era 1 (202 unbucketed) or Era 4 (57 unbucketed) are the real surfaces. |
| 2026-05-25 | Per-card sibling sweep — audit | **Iroh-pattern follow-up fully closed (7/7 cards wired) — earlier "remaining" callout was stale** | The 2026-05-24 row below originally flagged Archmage's Newt, The Fugitive Doctor, and Lost in Memories as the "remaining callouts await a small follow-up batch." Audit on 2026-05-25 (branch `dev/percard-stub-sweep-r60`) verified all three are in fact already wired in `internal/gameengine/per_card/tla_flashback_trigger_grants_r60.go` (canonical shape: `init() + AddResetHook`, `OnTrigger("Archmage's Newt", "combat_damage_player")` with `PermIsSaddled` cost branch, `OnTrigger("The Fugitive Doctor", "attacks")` with `attacker_perm == perm` gate + Clue sac + cost-4 grant, `OnTrigger("Lost in Memories", "combat_damage_player")` Aura with `AttachedTo` source-creature gating). 11 regressions in `tla_flashback_trigger_grants_r60_test.go` pass clean (Newt happy + saddled-zero + non-Newt-attacker + no-target; Fugitive happy + no-clue + non-self-attacker; Lost in Memories happy + non-enchanted-attacker + no-target; `GrantFlashbackUntilEOTWithCost` cost-override stamp). The 4 from PR `f73c5d1` (Snapcaster / Sphinx / Slickshot / Katilda and Lier) live in the sibling file `tla_flashback_single_target_r60.go`. Combined with the TLA mass-grant sweep (Lier / A-Lier / Return the Past / Backdraft Hellkite, r60 main), the Iroh-pattern continuous-effect family is now genuinely closed end-to-end across mass-grant + single-target + Aura-granted shapes. **No code change shipped under this audit row — purely a stale-callout correction.** |
| 2026-05-25 | Per-card sibling sweep — audit | **[SUPERSEDED by 2026-05-30 closure row above]** `removePermanent` API-misuse sweep status — 4 of 5 sibling sites still standing (etrata x2, bilbo x1, thassa x1; zabaz + zimone+dina clean) | Followup to the 2026-05-16 Muninn forensics (May-11 nil-deref) row that flagged 6 sibling handlers carrying the same `removePermanent + manual moveCardBetweenZones` anti-pattern as the Abdel Adrian crash. Current state on `dev/percard-stub-sweep-r60` audit: `etrata_the_silencer.go` still has 2 call sites of `removePermanent` immediately followed by `gameengine.DetachAll` (the 2026-05-23 `DetachAll` plumbing patched the AttachmentConsistency symptom but the API misuse remains — these sites still bypass §614 would-be-exiled replacements like Rest in Peace / Leyline of the Void, §903.9b commander redirect, and replacement-effect unregistering that `gameengine.ExilePermanent` handles canonically); `gen_bilbo_birthday_celebrant.go` and `thassa_deep_dwelling.go` both have 1 such site each; `zabaz_the_glimmerwasp.go` and `zimone_and_dina.go` are clean (no `removePermanent` call sites left). Total live work: 4 call sites across 3 handlers. Not crashing post-`b348f4a` and not currently surfacing invariant violations in Loki, so this is correctness-improvement work, not a fire — but the sweep is genuine and should not be conflated with the closed Iroh-pattern sweep. **No code change shipped under this audit row — recorded so future workers don't repeat the spelunk and so the gap stays visible.** |
| 2026-05-24 | Per-card sibling sweep | **Iroh-pattern follow-up — single-target trigger-grant family wired** (4 cards, with 3 more later confirmed wired — see 2026-05-25 audit row above) | Closes the "Single-target attack/ETB-triggered grants... is a separate systemic pattern — primitive (`GrantFlashbackUntilEOT`) already exists; per_card wiring is a follow-up sweep" callout from the 2026-05-23 TLA-flashback-grant entry. PR `f73c5d1` (per_card single-target trigger-grant sweep) shipped Snapcaster Mage, Sphinx of Forgotten Lore, Slickshot Lockpicker, and Katilda and Lier handlers — all reuse the existing `GrantFlashbackUntilEOT` primitive and pair an attack/ETB trigger with the per-source target picker. Combined with the TLA mass-grant sweep (Lier / A-Lier / Return the Past / Backdraft Hellkite, r60 main), the Iroh-pattern continuous-effect family is now well-covered end-to-end across mass-grant AND single-target shapes. (The original row claimed Archmage's Newt / The Fugitive Doctor / Lost in Memories still awaited a follow-up batch; the 2026-05-25 audit row immediately above confirms they were in fact already wired in `tla_flashback_trigger_grants_r60.go`.) |
| 2026-05-24 | Loki r41/r60 | **TriggerCompleteness (8) — trigger batch opened but never drained** (originally suspected as a missing-defer bypass). Root cause was actually two collaborating bugs: (a) the per-card dispatcher in `per_card/registry.go`'s `fireTrigger` increments a process-lifetime `gs.Flags["trigger_total"]` counter and silently short-circuits past 2000. With no per-turn reset the counter saturated by turn 40+ (Jaxis game 109 hit at turn 48 / 4357 events), permanently muting every subsequent dispatch — so creature-dies bearers like Jaxis emitted no `trigger_evaluated` follow-up event and the invariant flagged each remaining death. (b) The invariant itself treated every `sacrifice` event as a creature-dies candidate, so sacrificing a Plains / artifact / enchantment under a controller who happened to own a creature_dies bearer (Sefris, Syr Vondam Sunstar Exemplar) false-positived even when no trigger was expected. **Fix:** (1) `per_card/registry.go` — when the `trigger_depth > 8` or `trigger_total > 2000` guard short-circuits, emit a synthetic `trigger_evaluated` event with `capped="trigger_depth"`/`"trigger_total"` so the invariant's follow-up scan succeeds. (2) `phases.go` cleanup step (line ~324) — `delete(gs.Flags, "trigger_total")` so the counter is the intended per-turn runaway budget rather than an accidental lifetime cap. (3) `zone_change.go` `sacrificePermanentImpl` — add `was_creature: perm.IsCreature()` to the sacrifice event Details. (4) `invariants.go` `checkTriggerCompleteness` — skip `sacrifice` events whose Details say `was_creature=false`. 5 regressions in `internal/gameengine/trigger_cap_r60_test.go` (cleanup reset, non-cleanup persistence, cap-breaks-after-cleanup, invariant skips non-creature sacrifice, invariant still flags creature sacrifice). Loki re-run @ 2000 games / seed 41: TriggerCompleteness 8 → **0** (all 8 cleared in two passes: -2 from the per-turn reset, -6 from the type-aware filter). Goldilocks + full engine suite clean. |
| 2026-05-23 | Loki r41/r57 | **AttachmentConsistency (14→23 then bit-stable at 22 across r53→r57)** — Aura/Equipment `AttachedTo` pointing at a permanent no longer on any battlefield. Three bit-stable signatures: `"Ghoulish Impetus" attached to "creature token black zombie giant Token"`, `"Brilliant Wings" attached to "Tidal Warrior"`, `"Dub" attached to "creature token phyrexian mite Token"`. Root cause: several engine + per_card removal paths call `gs.removePermanent` / `removePermanentFromBattlefield` directly without `detachAll`, leaving auras/equipment with stale `AttachedTo` references in the window before SBA §704.5m/§704.5n run. The window is observed by `checkAttachmentConsistency` after `TakeTurn` returns but before the next `StateBasedActions` pass. **Fix:** (1) added `detachAll` inside `removePermanentFromBattlefield` (covers Craft, Meld, exile-self activation cost, Aura swap); (2) added it to the sweep alt-cost return path in `keywords_sweep.go` and both mutate arms in `keywords_batch6.go`; (3) exported `DetachAll` in `sba.go` for per_card callers; (4) added `gameengine.DetachAll` calls to the per_card flicker/blink/exile-self handlers that route through the package-local `removePermanent` helper: Displacer Kitten, Emiel the Blessed, Deadeye Navigator, Brago King Eternal, Thassa Deep-Dwelling, Yorion Sky Nomad, Etrata the Silencer, Wan Shi Tong, Jadzi Oracle of Arcavios, Bilbo Birthday Celebrant, Lord Xander (era3). Gain-control paths (`resolve.go:1585`, `resolve_helpers.go:4827`, Gilded Drake in `commander_staples.go`) intentionally do NOT detach — control change is not a zone change. 4 regressions in `internal/gameengine/attachment_consistency_r60_test.go` pin the `removePermanentFromBattlefield`, sweep-return, gain-control, and public-`DetachAll` paths. Loki re-run @ 500 games / seed 48 (and seed 41): AttachmentConsistency 22 → **0**, both seeds. Goldilocks: clean (15 ZoneCastGrantExpiry unrelated). |
| 2026-05-23 | Loki r41 | **ZoneCastGrantExpiry (8) — impulse-play / cast-from-exile grant outlived its declared expiry**. r59 already pinned the two `impulse_play` sites (the structured `resolveModificationEffect` arm at `resolve_helpers.go:1506` and the `resolveResidualByText` "you may play that card this turn" branch at `resolve_helpers.go:4760`). The remaining 8 violations came from two sibling arms in `resolveModificationEffect` that built `NewFreeCastFromExilePermission(...)` and left it duration-less: the **`heist`** arm (`resolve_helpers.go:354`, oracle "Until end of turn, you may play that card.") and the **`may_play_exiled_free`** arm (`resolve_helpers.go:550`, the "this turn" family — the parallel "as long as it remains exiled" wording is routed through `resolveResidualByText` and was already pinned). Shared helper `NewFreeCastFromExilePermission` is intentionally duration-less because Opposition Agent, Release to the Wind, and Dauthi Voidwalker all need "as long as exiled" / "while source in play" semantics, so the fix is per-call-site: stamp `Duration = "until_end_of_turn"` + `GrantTurn = gs.Turn` on the returned permission. 3 regressions in `internal/gameengine/zonecast_cast_from_exile_expiry_r60_test.go` pin both grants reclaim at `ExpireZoneCastGrants` cleanup, the `ZoneCastGrantExpiry` invariant stays clean, and the shared helper continues to default to no turn clock. See branch `dev/zonecast-grant-expiry-r60` |
| 2026-05-23 | Loki r44 / game 420 | **ZoneConservation "real cards disappeared"** — 28 hits in game 420 (Breya / Baron Bertram / Alela / SP//dr deck) at turn 10+, seat 3 census reads 4 cards low. Cards going MISSING (not duplicating), distinct from the r41–r43 paradigm-copy and Cerulean Sphinx duplication clusters. | Root cause was an Unholy Indenture self-reanimate driving SBAs past the pass cap, setting `gs.Flags["game_draw"]=1` (CR §104.4b mandatory-loop draw); `CheckEnd` did not react to `game_draw`, so the chaos turn loop pushed a fresh permanent spell onto `gs.Stack`, `ResolveStackTop` short-circuited on the `ended` flag, and the §727 no-op detector eventually fired `projectAndApply` — which truncated the stack via `gs.Stack = gs.Stack[:0]`, silently vanishing the `*Card`. **Fix (r59, commit `c711b1a`):** `evacuateStackSpellsToGraveyard` in `internal/gameengine/loop_shortcut.go` now routes the underlying `*Card` of each non-copy non-ability StackItem to its OWNER's graveyard (per CR §608.2g) before `projectAndApply` truncates `gs.Stack`. Regressions: `internal/gameengine/loki_r59_loop_evacuate_test.go` (4 tests pinning the primitive, owner-vs-controller routing, copy/ability skip, and end-to-end no-op-loop preservation). **Verification (r60, 2026-05-23):** Loki re-run @ 5000 games / seed 41 reports 0 ZoneConservation violations (was 102 in the original cluster, including the 28 game-420 hits); narrow reproduction `--games 421 --seed 41` confirms game 420 clean. Subsumed under the r59 merge `b186f89` (`-102 ZoneConservation`). |
| 2026-05-23 | Forge gauntlet | **TLA flashback-grant unimplemented (Iroh + sweep of similar patterns)** — Iroh's "during your turn, each non-Lesson i/s in your graveyard has flashback; Lessons have flashback {1}" was inert because the AST parser dumped the whole clause into a `phase_scoped_static` raw-text arg. Resolved in two steps: (r59) added the `GraveyardFlashbackGrant` predicate-style continuous-effect primitive (`internal/gameengine/keywords_flashback_grant.go`) registered against `CastFlashback`'s permission pipeline, with timestamp-driven LTB cleanup and an EOT orphan-source sweep — wired Iroh as the first consumer. (r60) one-shot variant `RegisterEOTGraveyardFlashbackGrant` for spells with no battlefield permanent — wired Past in Flames, Will of the Jeskai, the Flashback instant, and Recoup. **r60 TLA sweep** (this fix) ran the same pattern across the AST corpus, found 4 additional mass-grant cards with no per_card handler: Lier, Disciple of the Drowned (continuous always-on), A-Lier (continuous OnlyActiveTurn), Return the Past (Enchantment, continuous OnlyActiveTurn), Backdraft Hellkite (attack-trigger EOT mass grant). All four reuse `PrintedMassFlashbackCost` and the existing primitives — no further engine extensions needed. Lier's ETB+LTB lives in `custom_lier_disciple_of_the_drowned.go`; the other three in `internal/gameengine/per_card/tla_flashback_grants_r60.go`. 12 regressions in `tla_flashback_grants_r60_test.go` (ETB grant shape, active-turn gating where applicable, CastFlashback success, LTB / EOT-sweep cleanup, Backdraft attacker_perm filter). Single-target attack/ETB-triggered grants (Snapcaster, Katilda and Lier, Sphinx of Forgotten Lore, Slickshot Lockpicker, Archmage's Newt, The Fugitive Doctor, Lost in Memories) are a separate systemic pattern — primitive (`GrantFlashbackUntilEOT`) already exists; per_card wiring shipped as the Iroh-pattern follow-up (see 2026-05-24 + 2026-05-25 audit rows; all 7 now wired across `tla_flashback_single_target_r60.go` + `tla_flashback_trigger_grants_r60.go`) |
| 2026-05-20 | Loki r47 / game 333 | **God-Eternal Oketra CardIdentity zone-leak (5 of 5 shown details in r47, dominant new-after-Adric cluster)** — card "God-Eternal Oketra" appears in both seat 2 library and seat 2 graveyard. Root cause: the `god_eternal_tuck` arm of `resolveModificationEffect` (`internal/gameengine/resolve_helpers.go:4236`) called `removePermanent(src)` and inserted `src.Card` into the library at index 2. Oketra's "When this dies or is put into exile from anywhere, put it into your owner's library third from the top" fires AFTER the zone change, so by resolve time the card is already in the graveyard (or exile); `removePermanent` no-op'd against the now-off-battlefield Permanent and the card got duplicated into the library. Same anti-pattern as the Dread fix (2026-05-08) and Adric fix (commit `7e782cf`). **Fix:** mirror `shuffle_pronoun_into_owner_library`'s shape — capture `fromZone`, try `removePermanent` first, fall through to `removeFromZone` across each seat's graveyard → exile → hand, then insert at index 2 (clamped to tail when library < 3) and call `FireZoneChangeTriggers` with the real `fromZone`. 5 regressions in `internal/gameengine/god_eternal_tuck_r48_test.go` (battlefield, graveyard, exile, short-library, empty-library paths). Loki re-run @ 5000 games / seed 42: total 380 → **330** (−13%), CardIdentity 346 → **296** (−50, all Oketra hits gone), game 333 cleared. Next CardIdentity offender now Archon's Glory (1 game / 5 details). See commit on `dev/oketra-zone-fix-r48` |
| 2026-05-08 | Freya | **Commander-centric hand evaluation gap**: keepable hand heuristic didn't account for decks where the gameplan IS the commander (Voltron, high-synergy engine commanders) | Two-part fix in `cmd/hexdek-freya/advanced.go`. (1) Rewrote `computeOpeningHandSim` to model the deck with per-slot category flags (land/ramp/synergy/action) padded to `report.TotalCards` so basic lands actually appear in hand — the old simulation only sampled nonbasic profiles, depressing every keepable %. Standard "keepable" now also requires an actual action card (threat/removal/draw/combo/tutor/wipe/counter) instead of merely "any non-land". (2) Added `detectCommanderCentric` (Voltron archetype, ≥45% commander synergy, or commander oracle text containing ≥2 engine phrases) and a parallel `KeepableHandPctAdjusted` that accepts hands satisfied by ramp, a commander-synergy enabler, or natural land drops to commander CMC. Reports both metrics + avg turn to commander; exposed via deckprofile JSON and strategy JSON for hat consumption. Voltron Uril now reads 63% standard / 80% adjusted (was 37%) |
| 2026-05-08 | Hat | **Graveyard recursion intelligence gated behind ArchetypeReanimator**: non-reanimator decks with graveyard synergy (Szarekh, surveil, self-mill) got no strategic graveyard play | Added deck-agnostic `hasGraveyardRecursionValue` (flashback/unearth/escape/disturb/embalm/eternalize/encore/jump-start/aftermath/retrace/dredge) and `hasGraveyardRecursionEnabler` (Sun-Titan/Muldrotha-style) helpers; both ChooseDiscard and ChooseSurveil now bias recursion-bearing cards toward the graveyard regardless of archetype |
| 2026-05-08 | Goldilocks | **Dread — CardIdentity invariant violation**: card appeared in both library and graveyard after death-trigger shuffled it into owner's library | `shuffle_pronoun_into_owner_library` handler in `resolve_helpers.go` only removed the source from the battlefield. For "put into graveyard from anywhere" triggers (Dread, Purity, Guile, Vigor, Worldspine Wurm, etc.) the card was already in the graveyard at resolve time, so the library move left a stale graveyard reference. Handler now falls through to remove the card from graveyard / exile / hand if not on the battlefield, and threads the actual `from_zone` into `FireZoneChangeTriggers`. Verified clean on Dread + 8 related cards |
| 2026-05-08 | Goldilocks | **1,795 keyword_dead failures**: combat keywords scaffolded but no observable change | Two bugs in `cmd/hexdek-thor/goldilocks.go`. (1) `makeKeywordGameState` left `RetainEvents` at default false, so every `LogEvent` call (including the default-case `keyword_test` fallback) was dropped to `gs.lastEvent` and never reached `EventLog` — leaving `verifyKeyword`'s event-log probe blind. (2) Combat-relevant keywords (flying, trample, deathtouch, lifelink, first strike, double strike, vigilance, haste, menace, reach, defender, protection, infect, toxic, fear, intimidate, shroud, hexproof, indestructible, ward, horsemanship, flanking, skulk, all *walk variants) had no scaffold at all, so they fell through to a default that placed the card on the battlefield without exercising combat. Added `RetainEvents: true` plus a combat scaffold that puts the source on seat 0 attacking seat 1 with a 2/2 blocker, runs both first-strike and regular `DealCombatDamageStep`, then resets phase + clears combat flags so post-test `CombatLegality` doesn't fire on walls/defenders being tested. Result: keyword_dead 1,795 → 0, total goldilocks failures 1,915 → 54 |
| 2026-05-16 | Muninn | **May 11 grinder flood — 324 nil-deref panics in one run, identical signature**: `abdelAdrianETB → moveCardBetweenZones("battlefield"→"exile") → MoveCard → FireZoneChangeTriggers(perm=nil) → perm.Controller`. Symptom already patched in commit `b348f4a` (2026-05-08) by adding `perm != nil` guard around the LTB `FireCardTrigger`, but the API misuse remained: `abdel_adrian.go` was bypassing §614 would-be-exiled replacements, §903.9b commander redirect, aura `detachAll`, and replacement-effect unregistering. Root-cause fix in `internal/gameengine/per_card/abdel_adrian.go` — exile each pick through `gameengine.ExilePermanent(gs, p, perm)` (the canonical battlefield-exit API), drive token count off the survived-§614 count, drop the manual `removePermanent + moveCardBetweenZones` pair. Full forensic write-up at `docs/may11-nil-deref-forensics.md`, which also flags 6 sibling handlers (etrata, zabaz, zimone+dina, bilbo, thassa) carrying the same anti-pattern — they don't crash post-`b348f4a` but bypass the same machinery and should be swept in a follow-up. Same audit shows the 466 May 5 crashes (`ExtractPivot` at `tesla.go:55`, `winnerSeat=-1`) were already closed by commit `199837e` on 2026-05-05 — left documented to prevent re-investigation |
| 2026-05-19 | Loki r44 / game 404 | **Disruptive Pitmage hand+battlefield duplication (18 of r43's 410 CardIdentity hits)** — same anti-pattern as the Adric leak (commit `7e782cf`): `{T}: Counter target spell unless its controller pays {1}` made the creature look like a hand-castable counter response to `counterSpellEffect`, and `PriorityRound`'s response-cast path didn't remove the card from hand after paying cost. Independently rediscovered while investigating game 404; the Adric r44 fix landed in main during diagnosis and already covered both surfaces. **Additive refinement** on `dev/game-404-fix-r44`: `counterSpellEffect`'s Layout-1 (Activated AST) match now also requires `isEmptyActivationCost(a.Cost)` — defense-in-depth for the hypothetical instant/sorcery with a real-cost activated counter (`isPermanentSpell` already covers Pitmage; the empty-cost gate covers parser-artifact instants whose body is an Activated node, mirroring `collectSpellEffect`'s r41 follow-up gate). Plus 3 regression tests pinning the new gate. Loki re-run with both fixes (seed 41, 5000 games): game 404 18 → **0** violations |
| 2026-05-19 | Loki r43 / Krark zone-conservation | **Paradigm copy resolution leaks `*Card` refs into `gs.ParadigmExile` every tick — 824 ZoneConservation "extra real cards appeared" hits in r41 game 181 (Decorum Dissertation in a Krark, the Thumbless deck, seed 41 / turn 47 / 11 extras)**: Two bugs. (1) `ResolveParadigmCopies` in `internal/gameengine/phases.go` pushed the copy StackItem without `IsCopy: true`, so when the copy resolved as a non-permanent spell `ResolveStackTop` routed the copy `*Card` to the graveyard instead of letting CR §707.10 cease it. (2) The per-card OnResolve handlers for paradigm cards (Decorum Dissertation, Echocasting Symposium, Germination Practicum, Improvisation Capstone, Restoration Seminar — all 5 use the shared `paradigmExileItem` helper in `internal/gameengine/per_card/paradigm_echocasting_symposium.go`) ran a fresh `paradigmExileItem` call on every paradigm copy too, re-appending the copy's `*Card` to `gs.ParadigmExile` and re-flipping `CostMeta["exile_on_resolve"]=true`. `checkZoneConservation` counts `gs.ParadigmExile` contents as real cards, so each tick inflated the total by one. **Fix:** `IsCopy: true` on the copy StackItem so CR §707.10 ceasing applies; `paradigmExileItem` short-circuits when `item.IsCopy` (the original is the only Card paradigm-tracked; copies are transient). Regressions: `internal/gameengine/paradigm_r43_zone_test.go` + `internal/gameengine/per_card/paradigm_r43_test.go` (4 tests pinning both surfaces). Loki re-run: game 181 86 → **0** violations; full 5000-game fuzz: 1,255 → 1,113 (-11% vs r41 follow-up baseline / -33% vs r41 raw). ZoneConservation specifically: 824 → 664 (-19%). See commit on `dev/krark-zone-conservation-r43` |
| 2026-05-19 | Loki r41 follow-up | **Cerulean Sphinx zone-leak (1,622 of 1,652 r41 violations)**: `*Card` pointer duplicating across "owner's library" + "controller's battlefield" / "another seat's library". Two collaborating bugs. (1) `collectSpellEffect` (stack.go:604) returned the first Activated ability's effect as the cast-time spell body for ANY card with an Activated AST node, including permanent spells. Cerulean Sphinx's `{U}: This creature's owner shuffles it into their library` therefore ran at CAST resolution — putting the card into the owner's library — before `resolvePermanentSpellETB` then wrapped the same `*Card` in a fresh Permanent on the controller's battlefield. (2) The synthetic transient `Permanent` built in `ResolveStackTop` (stack.go:1109) and `resolveActivatedAbility` (activation.go:543) for handlers that need a `*Permanent` source omitted the `Owner` field, defaulting to seat 0 — so even when the card was owned by seat 1+, `shuffle_into_owner_library` routed to seat 0's library. Fix in `internal/gameengine/stack.go` + `activation.go`: `collectSpellEffect` now returns nil for permanent spells per CR §112.6/§603.5, and for instants/sorceries only matches `Activated` nodes with an empty cost (the parser-artifact spell-body shape that Summon the School / Divergent Growth / Eldrazi Confluence emit); both synthetic-Permanent sites now thread `Owner: item.Card.Owner`. Regression: `internal/gameengine/loki_r41_followup_test.go` (4 tests pinning both surfaces). Loki re-run: game 137 → 0 violations; full 5000-game fuzz: 1,652 → 1,255 (-24%, all remaining hits are unrelated token/copy and equipment-attachment clusters). See `docs/loki-r41-followup-report.md` |
| 2026-05-17 | Per-card tests | **`TestSai_ArtifactCastCreatesThopter` test pollution — Sai handler permanently stripped by `Reset()`**: bisected to `TestAllRegisteredTriggersAreDispatched` calling `per_card.Reset()`, which rebuilt the global registry from `registerDefaults()` only. Sai was registered via `tribal_lords.go`'s `init()`, which Go runs exactly once per process, so post-Reset the registry permanently lost Sai (plus ~50 other handlers wired via sibling `init()` functions in `obeka_support.go`, `combat_restrictions.go`, `batch17_sweep.go`, and four `zz_*_register.go` files). The `zz_*` files already exposed `Register*` functions but no test was wired to invoke them after Reset. **Fix:** added `AddResetHook(fn)` in `registry.go`; `Reset()` now re-invokes every registered hook on the fresh registry. Each init() that populates the registry now adds a hook (extracted bodies into named `registerXxx` functions where needed). Sibling latent bug surfaced + fixed: `custom_saheeli_radiant_creator.go` registered `OnTrigger("...", "begin_combat", ...)` but the engine fires `combat_begin` — added `"begin_combat": {"combat_begin"}` alias in `event_aliases.go` |
